# Remediation Plan

This file remains mostly empty until findings are created. Do not add
remediation items that are not tied to cold-audit findings or explicit
notes.

## Prioritization Rules

1. Critical and high findings that affect market-data correctness,
   security, customer trust, or live operations come first.
2. Medium findings that can silently corrupt data outrank obvious
   visible failures.
3. Docs fixes follow code fixes unless the doc defect itself creates
   immediate operator/customer risk.
4. Competitive product gaps are prioritized by customer-facing impact,
   not implementation ease.

## Register

| Remediation ID | Finding IDs | Priority | Owner | Proposed Fix | Verification | Status |
| --- | --- | --- | --- | --- | --- | --- |
| R-1201 | F-1201 | P0 | ops/security | Finish the R1 firewall reconciliation rather than treating the current rollout as closed: preserve the new default-drop posture for internal services, then resolve the live/repo drift around captive-core ingress and public `11726/tcp`. | Current external probes now time out for `9000`, `9001`, `9090`, `3100`, `9080`, `9100`, `6061`, and `38563`; `nftables` is active and UFW inactive. Closure still requires either removing public `11726/tcp` exposure or documenting/proving it as intentional in source config plus runbooks. | open |
| R-1202 | F-1207 | P0 | web/security | Preserve the completed Next.js/Dependabot config improvements and finish the remaining hosted repository alert posture. | Current apps pin `next@15.5.18`, every web app has a Dependabot npm entry, and `pnpm audit --audit-level high` now returns only one moderate advisory per app. Closure still requires enabling repository vulnerability alerts and Dependabot alerts, plus deciding whether the lingering `eslint-config-next@15.0.4` pin should move with the Next baseline. | open |
| R-1203 | F-1203, F-1204 | P0 | api/web/release | Finish the endpoint-removal cleanup that remains after the live migration: refresh the stale explorer generated TypeScript client, migrate `scripts/dev/audit-public-api.sh` off removed legacy routes, and remove/update machine-facing `llms.txt` advertising `/v1/coins`. | `bash scripts/dev/audit-public-api.sh` passes against current R1 without legacy-route probes; `pnpm generate:api` produces a clean diff; `rg` finds no active machine-facing advertisement for removed legacy routes except explicitly historical docs/comments. | fixed |
| R-1204 | F-1205 | P0 | ops | Finish the evidence-timer rollout by enabling or intentionally replacing `sla-probe.timer`, while preserving the now-live archive/supply timer coverage. | R1 now shows enabled `archive-completeness.timer`, `verify-archive-tier-a.timer`, and `supply-snapshot.timer`; closure requires `sla-probe.timer` enabled with a valid operator key, or a documented replacement plus alert/runbook reconciliation. | open |
| R-1205 | F-1206 | P0 | release/ops | Block public flip on `verify-launch-ready`; either complete L4/L5/L6 blockers or explicitly defer with signed owner/risk acceptance. | `go run ./scripts/ci/verify-launch-ready` passes, or release notes include accepted deferrals and compensating controls. | open |
| R-1206 | F-1208, F-1210 | P1 | ingestion/api/ops | Add readiness/status semantics for enabled-source freshness and external-source degradation, or add an explicit `/v1/livez` vs `/v1/readyz` split with status-page mapping. | Prometheus source-stopped/freshness alerts line up with status page and readiness policy; tests cover degraded source behavior. | open |
| R-1207 | F-1209 | P1 | ops | Investigate memory pressure/swap exhaustion and MinIO growth; add capacity SLO thresholds and runbook proof. | Memory alert cleared or justified; swap not fully consumed; MinIO retention/capacity plan documented and alerting verified. | open |
| R-1208 | F-1203, F-1207 | P1 | ci/release | Add CI gates for generated artifacts and web dependency advisories to the same branch-protection-required checks, and add npm/pnpm Dependabot update entries for every web app. | PR with stale generated artifacts or critical/high web advisory fails required checks; Dependabot opens web dependency PRs. | open |
| R-1209 | F-1211 | P1 | ops/comms/web | Replace stale Upptime/cstate status-page procedures with the actual `web/status` incident workflow, or implement the missing incident-source bridge if Upptime remains desired. Add a drill that opens, updates, and resolves a test incident end-to-end. | No active docs reference removed `deploy/status-page/cstate/**`, Upptime issue automation, or the retired `RatesEngine/ratesengine-status` issue flow; status-page drill produces a visible incident on the deployed/preview status page; comms templates point to the same incident ID/source. | fixed |
| R-1210 | F-1212 | P0 | dashboard/billing/api | Clamp dashboard-minted key budgets by account tier before persistence so free or lower-tier accounts cannot store paid-tier budgets. | `TestHandleCreate_TierClampsRateLimit` covers the tier ladder; dashboard-created Free keys persist at 60/min, Starter at 1000/min, Pro at 10,000/min, Business at 60,000/min, and Enterprise at 100,000/min. | fixed |
| R-1211 | F-1213 | P0 | aggregate/market-data | Retained for audit history. The min-volume gate now uses normalized USD totals computed before proxy pair rewrites, preserving the correct 7-decimal treatment for configured classic Stellar USD pegs. | The dedicated classic-USDC `$10k` regression passes under `MinUSDVolume=10000`; the original undercount no longer reproduces. R1's current `min_usd_volume=0` remains an explicit operator posture, not evidence that this arithmetic bug persists. | fixed |
| R-1212 | F-1214 | P0 | repo-admin/security | Enable enforceable branch protection/rulesets for `main`: required checks, CODEOWNER review, signed commits, no force pushes, no direct pushes, and protected release tags. Upgrade repo plan or public-flip if needed to make protections available. | `gh api repos/RatesEngine/rates-engine/branches/main --jq .protected` returns true and protection/ruleset APIs show required checks/reviews/signatures. | open |
| R-1213 | F-1215 | P0 | repo-admin/ops | Add required reviewers and branch/tag restrictions to all production deployment environments, disable admin bypass where available, and scope secrets to the environments that need them. | GitHub environment API shows non-empty protection rules for `r1`, `explorer-production`, `docs-production`, `status-production`, and `github-pages`; test workflow dispatch pauses for approval. | open |
| R-1214 | F-1216 | P1 | repo-admin/security | Restrict GitHub Actions to selected trusted actions and pin non-GitHub-owned actions to immutable SHAs; add workflow linting for unpinned external `uses:` entries. | The new PR-diff lint blocks fresh mutable third-party tags, but closure still requires hosted-policy hardening and removal/SHA-pinning of the 12 currently tag-pinned third-party workflow uses. | open |
| R-1215 | F-1217 | P1 | api/security | Make SEP-10 replay protection mandatory when SEP-10 token issuance is enabled: require Redis replay guard or add a Postgres guard fallback, and fail startup for `auth_mode=sep10` without a guard. | Current workspace now fails startup for `auth_mode=sep10` without Redis; replay guard remains wired when Redis is present. | fixed |
| R-1216 | F-1218 | P0 | api/security/billing | Stop returning usable API keys from unverified email-only signup. Require magic-link/email verification or an authenticated account session before key creation, while preserving the now-atomic Redis same-email reservation. | Wave 45 has landed the verifier marker, opt-in request-path gate, tests, and config docs. Closure still requires the ownership-proof posture to be enabled on the production/public path, or otherwise made the default security contract rather than remaining `signup_require_email_verification=false` with R1 unset. | open |
| R-1217 | F-1219 | P0 | billing/platform/api | Retained for audit history. Finish moving Stripe webhook side effects onto a coherent upgrade contract across Redis legacy keys, Postgres dashboard-created keys, and platform billing/account rows, while preserving the current event-log dedupe/audit path. | Wave 55 closes the missing dashboard-key leg: production `Platform` wiring now includes `postgresstore.NewAPIKeyStore(pgStore)`, the webhook upgrades active Postgres keys idempotently alongside Redis signup keys, and focused platform-webhook regression tests pass. Residual best-effort Postgres sync failures are now explicit via `ratesengine_stripe_platform_sync_errors_total{operation=...}` plus the new operator runbook. | fixed |
| R-1218 | F-1220 | P0 | release/ops/db | Add migration handling to tagged deploys: ship the migration directory/release artifact, run `ratesengine-migrate up` before binary restart, and/or gate readiness on embedded expected schema version. | The workspace now stages tag-matched migrations plus a pre-swap migration task, but closure still requires a runnable deploy job: either install `ansible.posix` alongside `ansible-core` or replace `ansible.posix.synchronize`, then prove the workflow/playbook path executes end to end. | fixed |
| R-1219 | F-1221 | P2 | docs/release | Reconcile release/deploy docs with the binary-only release workflow, or restore GHCR image publishing if containers are intended artifacts. | Docs and release workflow agree; release dry-run/checklist verifies exactly the artifacts published for a tag. | fixed |
| R-1220 | F-1222 | P1 | ops/release | Rewrite fallback rollback instructions around the actual `/usr/local/bin/<binary>.prev-<tag>` backups and `/var/lib/ratesengine/deployed-versions/<binary>` sidecars. | A rollback drill from the docs succeeds on a non-production target; no rollback docs reference `/opt/ratesengine/release-<tag>` unless that directory is actually created. | fixed |
| R-1221 | F-1223 | P0 | ops/security | Retained for audit history. The reviewed Caddyfile is now live on R1, including Cloudflare trusted-proxy handling, `{client_ip}` forwarding, and public `/metrics` blocking. | External `/metrics` now returns `404`, and the live Caddyfile contains the trusted-proxy/client-IP block. Remaining drift-monitoring improvements are useful follow-up, but the finding's concrete exposure is fixed. | fixed |
| R-1222 | F-1224 | P1 | dashboard/security | Use middleware-resolved client IP for dashboard magic-link, user, and session audit writes instead of parsing `r.RemoteAddr`. | Tests prove proxied requests store the XFF client IP, direct requests store socket peer, and invalid forwarded headers fall back safely. | fixed |
| R-1223 | F-1225 | P0 | api/market-data | Finish source/live reconciliation for the since-inception USD fallback. Current source and tests implement it, but public R1 still serves the stale empty-series behavior. | Closure requires public R1 `native/fiat:USD` since-inception to return the direct Circle-USDC daily series under the configured peg list, with deployment/version evidence showing the live path matches source. | open |
| R-1224 | F-1226 | P0 | platform/api/security | Retained for audit history. Finish request-aware key policy enforcement for IP allowlist, referer allowlist, permissions/scopes, monthly quotas, and debounced usage updates. | Current committed source now ships cache-stable policy enforcement, positive monthly-quota gating with per-key/account override propagation, and the Redis-debounced Postgres `TouchUsage` path wired through API startup/server assembly. The original enforcement defect is fixed; remaining account-usage documentation/model drift stays tracked under `F-1259`. | fixed |
| R-1225 | F-1227 | P1 | docker/db | Make the migrate image self-contained by copying `migrations/` into a stable runtime path and defaulting/documenting `-migrations` to that path, or document a required read-only bind mount and enforce it in Compose/Kubernetes examples. | `docker run ... ratesengine-migrate status` reaches the database/version check using the packaged or mounted migration path; `make smoke-docker` fails if the migration source cannot be opened. | fixed |
| R-1226 | F-1228 | P0 | api/streaming/ops | Finish source/live reconciliation for the SSE deadline fix. Current source now clears the per-request SSE write deadline with `http.ResponseController`, but live R1 still behaves like the old cutoff path. | Closure requires a public R1 probe that keeps `/v1/price/tip/stream` open beyond 60 seconds with heartbeats/events, proving the deployed runtime actually carries the source fix. | open |
| R-1227 | F-1229 | P1 | ops/api | Fix `scripts/dev/verify-cdn.sh` to use valid `asset=` query parameters, assert the intended SSE cache header, and read enough of the stream body to prove the edge is not buffering. | Running the script against R1 passes for valid endpoints; deliberate CDN buffering/cache misconfiguration causes a failing check. | fixed |
| R-1228 | F-1230 | P0 | data/backfill/api | Complete verified historical backfill for launch-critical pairs, beginning with XLM/USDC and XLM/USD fallback pairs; expose coverage metadata such as `earliest_available_at` and backfill status in history responses or adjacent diagnostics. | R1 `native`/Circle-USDC daily history starts at least one year before current date or accepted inception target; API response exposes coverage bounds; launch docs no longer imply deeper history than exists. | open |
| R-1229 | F-1231 | P0 | repo-admin/ci | Restore full `ci` on pushes to `main` until branch protection enforces PR-only merges, or immediately enable protection/rulesets requiring the canonical CI jobs before merge. | Current `ci.yml` now includes `push: branches: [main]`, closing the direct-main workflow trigger gap. | fixed |
| R-1230 | F-1232 | P0 | api/market-data | Make declared USD-pegged stablecoins first-class in `/v1/price` and `/v1/price/batch`, sharing the same source/enrichment semantics as `/v1/assets/{id}.price_usd`. | R1 returns a populated row for Circle USDC on single, GET batch, POST batch, and asset-detail price surfaces; tests assert all four agree within the chosen precision. | fixed |
| R-1231 | F-1233 | P0 | ingest/backfill/sdex | Add support for legacy `ClaimAtomTypeV0` in the SDEX decoder, including raw Ed25519-to-G-address seller conversion and fixtures covering pre-P17/pre-P18 claim atoms. Ensure unsupported claim variants either increment decode-error metrics or hard-fail backfill ranges rather than disappearing inside the adapter. | Current committed code decodes V0 claim atoms into canonical trades by deriving the seller G-strkey, and targeted SDEX tests pass on that path. | fixed |
| R-1232 | F-1234 | P1 | oracle/observability | Add explicit unknown-symbol/feed observability for Reflector, Redstone, and Band partial-batch skips, and reconcile configured upstream feed lists against canonical asset allow-lists. | Mixed known/unknown oracle batch tests increment a per-source unknown-feed counter while still landing known entries; dashboards/runbooks show the unknown feed IDs; a feed-list reconciliation job fails or alerts when upstream coverage exceeds canonical coverage. | fixed |
| R-1233 | F-1235 | P1 | external/observability | Wire websocket streamer parse failures into source-labelled metrics for Binance, Kraken, Bitstamp, and Coinbase, and align external connector docs/runbooks with the metric actually emitted. | Current `HEAD` increments `SourceDecodeErrorsTotal` in each streamer parse-error branch; targeted streamer package tests pass. | fixed |
| R-1234 | F-1236 | P0 | supply/data-quality | Gate supply snapshots on component freshness: retain the stale-component rejector plus committed classic/SEP41/XLM producer paths, then remove or explicitly model the remaining zero-value permissive bypasses. | Current source now has producer coverage for classic, SEP-41, and live-reader XLM freshness, and the targeted supply/timescale/aggregator/indexer test set passes. Closure still requires a policy decision for static-XLM fallback publishing, stopping classic/SEP-41/XLM freshness-query failures from silently collapsing to `0`, and storage-backed stale-reader tests proving snapshots reject or degrade explicitly rather than re-entering the permissive bypass. | fixed |
| R-1235 | F-1237 | P1 | external/identity | Use verified CoinMarketCap numeric IDs in the CMC poller and keep ticker-symbol lookup only as a documented fallback for catalogue entries without IDs. | Indexer wiring now passes `Catalogue.CoinMarketCapIDs()`, but closure still requires the ops verification path to do the same and committed ID-mode fixtures to prove numeric-ID responses map back to the intended asset. | fixed |
| R-1236 | F-1238 | P1 | api/streaming/ops | Gate `redispub.NewSubscriber` on `rdb != nil` and define the no-Redis stream behavior explicitly, either by leaving Hub nil for a 503 or by serving heartbeat-only degraded streams. | Current workspace now gates subscriber creation on `rdb != nil && hub != nil`; targeted API package tests pass with the new wiring. | fixed |
| R-1237 | F-1239 | P1 | ops/data-quality | Validate `-progress-every > 0` for `wasm-history` and `extract-wasm-from-galexie`, or deliberately support zero by avoiding modulo and treating it as "no periodic progress". | Unit tests cover `progress-every=0` and nonzero progress for both WASM tools; a short fixture walk completes and emits final artifacts without panic. | fixed |
| R-1238 | F-1240 | P1 | docker/release | Align every Dockerfile `FROM golang:` tag with `go.mod`/release, or explicitly document and test supported toolchain skew. Add a lint check that fails on Dockerfile Go-version drift. | `rg "FROM golang:" docker` matches the Go version resolved by `go env GOVERSION` from `go.mod`; Docker README, CI, and release workflow agree; `make build-docker` succeeds under the pinned tag. | fixed |
| R-1239 | F-1241 | P2 | db/docs/ops | Bring `migrations/README.md` up to date through the latest numbered migration and add a docs drift guard that compares the listed migration numbers against on-disk `.up.sql` families. | README lists every migration family present on disk; an omitted future migration fails docs/CI validation; migration round-trip test remains green. | fixed |
| R-1240 | F-1242 | P1 | aggregate/storage/product | Either persist real per-source USD contribution volume into `price_source_contributions.volume_usd`, or remove/defer the field and its product promise until a writer exists. | Current committed code carries per-trade USD attribution through the filter chain and persists only post-filter survivor dollars per source; targeted aggregator/orchestrator tests pass. | fixed |
| R-1241 | F-1243 | P0 | storage/assets/data-quality | Retained for audit history. Finish closure proof for the repaired classic-asset registry write contract after the TTL dedupe and `RowsAffected` source fixes landed. | Wave 64 adds `TestAssetRegistry_DuplicateReplayDoesNotMutateCounters`, which clears the dedupe cache between insert/replay operations to model a fresh-store replay boundary and proves exact replay stays idempotent while new same-asset trades still advance counters. The tagged integration test passes on current head. | fixed |
| R-1242 | F-1244 | P0 | security/platform/webhooks | Retained for audit history. Reconcile the platform security spec with the shipped webhook-secret storage model: either implement the claimed sealed-box / managed-master-key protection, or revise the architecture prose to the actual recoverable-`bytea` posture and document that boundary honestly. | Wave 54 revises the architecture spec to the recoverable-`bytea` model already reflected by code/comments; the false sealed-box promise no longer survives on the reviewed surfaces. | fixed |
| R-1243 | F-1245 | P0 | security/platform/webhooks | Enforce webhook egress policy at create/update and send time. Reject private/reserved/loopback/link-local destinations, userinfo, and unsafe ports; constrain or disable redirects; re-resolve destinations before delivery to address DNS rebinding. | Current workspace now validates internal/private destinations at registration, re-resolves before dial, and disables redirect following; targeted webhook tests pass. | fixed |
| R-1244 | F-1246 | P2 | docs/api/product | Bring `docs/reference/api-design.md` back into line with the shipped dashboard webhook surface, including the distinction between server-sent streaming and registered outbound callbacks. | The API-design reference now says webhook callbacks shipped and explains the webhook/SSE split. | fixed |
| R-1245 | F-1247 | P0 | platform/webhooks/ops | Introduce queue-claim semantics for webhook deliveries before outbound POSTs so multiple API workers cannot send the same pending row concurrently. | Current `HEAD` claims rows with `FOR UPDATE SKIP LOCKED`, advances `next_attempt_at` by a lease interval before send, and preserves retry completion paths. | fixed |
| R-1246 | F-1248 | P1 | platform/webhooks | Retained for audit history. Finish closure proof for the new advisory-lock webhook quota path. | Wave 46 clears the migration bootstrap blocker, and the isolated concurrent webhook-cap integration scenario now passes. | fixed |
| R-1247 | F-1249 | P0 | platform/webhooks/product | Implement the missing event-producer/fan-out path for customer webhooks so declared incident/anomaly/divergence events actually create queue rows for subscribed hooks. | All four declared event families now have production enqueue paths: `anomaly.freeze` + `divergence.firing` from the aggregator hot paths; `incident.sev1` + `incident.resolved` from the operator-triggered `ratesengine-ops emit-incident` command against the embedded incident corpus at `internal/incidents/data/`. F-1249 marked `fixed` in the findings register; remediation plan caught up by wave 97. | fixed |
| R-1248 | F-1250 | P1 | aggregate/storage/anomaly | Enforce a database-backed "at most one open freeze row per pair" invariant and make the writer rely on that invariant under concurrent refreshes. | `freeze_events.RecordFreeze` runs check + insert inside a transaction guarded by `pg_advisory_xact_lock(hash(asset, quote))`; concurrent same-pair freezes serialise through one critical section so only one open durable row exists per still-firing pair. F-1250 marked `fixed` in the findings register; remediation plan caught up by wave 97. | fixed |
| R-1249 | F-1251 | P0 | storage/indexer/data-quality | Retained for audit history. Finish closure proof for the repaired FX resolver path after the source-level trade-time freshness, NUMERIC text canonicalisation, and freshness sentinel fixes. | Wave 46 clears the migration bootstrap blocker, and `TestVWAPUSDFXResolver_QueriesPrices1m` now passes end to end against a fully migrated integration database. | fixed |
| R-1250 | F-1252 | P2 | docs/ops/release | Reconcile the multi-region cutover checklist with the actual consistency-check command surface. | `Makefile::verify-cross-region` exists and shells to `scripts/dev/verify-cross-region.sh`; operators following the cutover runbook get the consistency check the docs promised. F-1252 marked `fixed` in the findings register; remediation plan caught up by wave 97. | fixed |
| R-1251 | F-1253 | P0 | ops/security/config | Couple Redis ACL lockdown to rendered application ACL usernames and verify the exact generated config during rollout. | Current Ansible rendering emits `redis_username` under lockdown and matches the named ACL-user contract consumed by the Redis client builder. | fixed |
| R-1252 | F-1254 | P0 | ops/security/config | Reconcile the Redis ACL key allow-list with the live application namespace inventory and automate drift detection. | Current ACL rendering includes the live `rl:*`, `sub:*`, `signup:email:*`, `sep10:seen:*`, `usage:*`, `assets:list:*`, and `markets:list:*` families. | fixed |
| R-1253 | F-1255 | P1 | platform/auth/data-quality | Make first-login provisioning transactional or idempotent per normalized email so concurrent valid callbacks converge on one durable account/user pair. | Redis-SETNX-backed `EmailLocker` seam wired through `dashboardauth.Config`. The losing callback short-circuits before `Account.Create`, polls briefly for the winner's user, and never inserts a speculative-account row. Redis-less deployments fall back to the Suspend-on-conflict path as defence in depth. `signup:lock:*` added to the Redis ACL allow-list. F-1255 marked `fixed` in the findings register; remediation plan caught up by wave 97. | fixed |
| R-1254 | F-1256 | P2 | dashboard/docs/product | Retained for audit history. Reconcile dashboard and OpenAPI key-budget copy with the tier-clamped backend behavior. | OpenAPI now documents the tier-clamp ladder and wave 48 updates the dashboard hint to the same persisted-budget semantics. The source surfaces no longer advertise the stale free-tier `1000` shorthand. | fixed |
| R-1255 | F-1257 | P1 | platform/keys | Retained for audit history. Finish closure proof for the new advisory-lock API-key quota path. | Wave 50 clears the product blocker, wave 53 repairs both invalid fixture fields, and the full `TestPlatformPostgresStores` integration now passes through the intended concurrent cap proof. | fixed |
| R-1256 | F-1258 | P0 | api/ops/runtime | Make Redis-less usage tracking genuinely disabled or nil-safe so optional Redis deployments cannot panic on authenticated traffic. | Wave 33 (2026-05-12) replaces the unconditional `UsageReader: usageReaderAdapter{c: usageCounter}` with `UsageReader: usageReaderOrNil(usageCounter)`. The helper returns a typed-nil `v1.UsageReader` when the counter is nil; the `/v1/account/usage` handler already short-circuits on `usageReader == nil` with an empty list, so Redis-less deployments degrade cleanly instead of nil-deref'ing on `Read`. F-1258 marked `fixed` in the findings register; remediation plan caught up by wave 97. | fixed |
| R-1257 | F-1259 | P2 | docs/api/product | Retained for audit history. Reconcile `/v1/account/usage` docs, comments, generated references, and the tracked Postman collection with the current Redis-backed live path. | Current tracked artifacts now agree across source OpenAPI, handler comment, API-design reference, explorer inventory, generated YAML, `/readyz` wording, and `examples/postman/rates-engine.postman_collection.json`. | fixed |
| R-1258 | F-1260 | P0 | aggregate/market-data | Retained for audit history. The implementation now evaluates `aggregate.min_usd_volume` against the post-class/post-outlier survivor set that reaches VWAP publication. | Current code routes the survivor-only USD total through `dropForMinUSDVolume`, and the dedicated regression test proves discarded pre-filter volume no longer satisfies the threshold. | fixed |
| R-1259 | F-1261 | P0 | db/release/ops | Retained for audit history. Rewrite `0030_asset_supply_history_unique_constraint` so it can safely alter the compressed `asset_supply_history` hypertable. | Wave 46 applies the decompress/disable-DDL/restore-compression sequence and migration round-trip now passes on current head. | fixed |
| R-1260 | F-1262 | P0 | platform/keys/db | Retained for audit history. Normalize API-key allowlist zero values before Postgres insert/update so omitted dashboard fields persist as empty arrays rather than SQL NULL. | Wave 50 lands `nonNilStringArray` across create/update and closes the product bug; the full integration no longer fails on `referer_allowlist`. | fixed |
| R-1261 | F-1263 | P1 | platform/tests/evidence | Retained for audit history. Repair the concurrent API-key quota integration fixture so it generates schema-valid `kid_<hex>` IDs and `rek_<8hex>` stored prefixes matching production, then rerun the full Postgres-store proof. | The repaired fixture reaches quota assertions instead of `api_keys_{id,key_prefix}_check`, and the full Postgres-store integration is green. | fixed |
| R-1262 | F-1264 | P1 | ops/docs/security | Reconcile Prometheus/Loki operator docs with the actual R1 firewall/access posture. Describe current listeners separately from public reachability, keep SSH tunnel guidance authoritative, and remove or qualify future-Caddy claims if they are no longer the chosen exposure model. | Current README prose now separates on-host listeners from firewall-blocked ingress and keeps SSH tunnelling as the supported access path. | fixed |
| R-1263 | F-1265 | P2 | ops/docs/monitoring | Remove the stale claim that the Ansible Alertmanager template still uses `critical/warning/info`; update the standalone config header, Alertmanager README migration note, and Prometheus role README to describe the already-shared `page/ticket/informational` ladder. | Alertmanager README + standalone YAML comment + Prometheus role README now agree with the actual template on the shared severity ladder. | fixed |
| R-1264 | F-1266 | P1 | ops/docs/deployment | Reconcile the top-level Ansible README with the landed role inventory and the real archival-node observability implementation state. | README now names the landed roles/playbooks and states the archival-node Promtail/Loki path remains deferred until the TODO-backed task wiring lands. | fixed |
| R-1265 | F-1267 | P1 | ops/docs/monitoring | Reconcile every Healthchecks setup path to the current five-check contract and explicitly include the SLA probe URL wherever the external checks are enumerated. | README, installer comment, and pre-launch hardening doc all say five checks and include `HEALTHCHECKS_URL_SLA_PROBE`. | fixed |
| R-1266 | F-1268 | P1 | ops/docs/monitoring | Align the R1 rules README copy path with the actual `prometheus.r1.yml` `rule_files` path, then lock the two together with a simple docs/config drift check. | Operators copying exactly from the README land files in the directory Prometheus actually loads; grep finds no conflicting `/rules.d/` vs `/rules.r1/` guidance on this surface. | fixed |
| R-1267 | F-1269 | P2 | ops/docs/audit-tooling | Reconcile the WASM audit-input README with the curated YAML that future audit refreshes actually maintain. | README describes the current eight named source blocks and does not imply `_unattributed` remains part of the live schema after the 2026-05-01 correction. | fixed |

## Verification Template

Each remediation must state:

- code files changed
- tests added or updated
- commands run
- migration or deployment impact
- docs updated
- R1/runtime validation required or not required
- rollback plan
