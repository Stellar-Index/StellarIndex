# Cross-File Interaction Ledger

Use this ledger for material coupling. IDs start at `XFI-0001`.

## Register

| ID | Status | Interaction | Source Files | Target Files | Contract | Failure Modes | Tests/Evidence | Findings |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| XFI-0001 | open | R1 firewall design -> installed host firewall -> external exposure | `configs/ansible/roles/archival-node/tasks/11-firewall.yml`; `configs/ansible/roles/archival-node/templates/nftables.conf.j2`; `configs/ansible/roles/archival-node/defaults/main.yml` | R1 `nftables`, `ufw`, `ss`, public TCP reachability | nftables is enabled, UFW disabled, input policy default-drop, and only SSH/publicly intended ports are reachable from the internet; internal services restricted to `internal_cidrs`. | nftables inactive/empty, UFW inactive, wildcard service listeners are externally reachable. | R1-0005; R1-0006; R1-0008; EV-0019 | F-1201 |
| XFI-0002 | open | API route table -> OpenAPI source -> generated reference -> generated TS types -> frontend/status/examples/smoke scripts | `internal/api/v1/server.go`; `internal/api/v1/coins.go`; `internal/api/v1/currencies.go`; `openapi/rates-engine.v1.yaml` | `docs/reference/api/rates-engine.v1.yaml`; `web/explorer/src/api/types.ts`; `web/explorer/**`; `web/status/**`; `examples/**`; `scripts/dev/audit-public-api.sh`; `scripts/dev/r1-smoke.sh` | Removed HTTP routes must disappear from generated artifacts, active consumers, smoke/audit scripts, and production deployment in the same change. | Generated artifacts drifted; active consumers and smoke/audit scripts still call removed paths; deployed R1 still serves old path, so local source and runtime disagree. | EV-0010; EV-0011; EV-0012; EV-0020; R1-0001 | F-1202; F-1203; F-1204 |
| XFI-0003 | open | Operational timer files -> install/deploy path -> R1 installed timers -> monitoring rules/runbooks | `deploy/systemd/sla-probe.timer`; `deploy/systemd/verify-archive-tier-a.timer`; `deploy/systemd/supply-snapshot.timer`; `deploy/systemd/archive-completeness.timer`; `deploy/monitoring/rules/*.yml`; `docs/operations/runbooks/*` | R1 systemd units/timers; Prometheus textfile metrics | SLA, archive verification, archive completeness, and supply-snapshot timers are installed and produce evidence metrics expected by alert rules and runbooks. | Timer unit files exist in repo but are not installed on R1; monitoring/readiness can report partial health while evidence-producing jobs are absent. | R1-0002; R1-0003; R1-0004 | F-1205 |
| XFI-0004 | open | Launch-readiness backlog -> CI/local verify target -> public flip decision | `scripts/ci/verify-launch-ready`; `docs/architecture/launch-readiness-backlog.md`; `Makefile` | local/CI release decision process | A public flip should not proceed while launch readiness rows are red. | Canonical `make verify` passes while `verify-launch-ready` fails; developers can have a green pre-push gate with known public-launch blockers. | EV-0009; EV-0013 | F-1206 |
| XFI-0005 | open | Web package pins -> CI/dependabot coverage -> hosted dependency alerts -> deployed web apps | `web/explorer/package.json`; `web/dashboard/package.json`; `web/status/package.json`; `.github/workflows/ci.yml`; `.github/dependabot.yml` | Cloudflare Pages builds and public explorer/dashboard/status pages | Web apps should receive automated dependency updates, hosted vulnerability alerting, and CI should fail on critical/high web advisories. | All web apps pin vulnerable `next@15.0.4`; CI has no `pnpm audit`; Dependabot omits npm/pnpm ecosystems; hosted vulnerability and Dependabot alerts are disabled. | EV-0014; EV-0051; `.github/workflows/ci.yml`; `.github/dependabot.yml` | F-1207 |
| XFI-0006 | open | R1 source health -> API readiness/status -> customer/SLA truth | R1 indexer/aggregator/API logs; Prometheus alerts | `/v1/healthz`; `/v1/readyz`; status page probes; runbooks | Health/readiness/status should represent material ingest and latency degradations or deliberately scope themselves to process liveness only. | Health/ready were ok while multiple source-stopped, latency, and memory alerts were firing. | R1-0009; R1-0010 | F-1208; F-1209; F-1210 |
| XFI-0007 | open | Status-page implementation -> incident runbook -> customer communication templates | `web/status/**`; `deploy/status-page/README.md`; `docs/operations/status-page-setup.md`; `docs/operations/runbooks/sev-status-page-update.md`; `deploy/comms/README.md`; `deploy/comms/incident-update.md` | `status.ratesengine.net` incident publication workflow | Operators should have one current, executable status-page path for opening/updating incidents, and customer-comms templates should point to the same incident system. | Shipped implementation is a Cloudflare Pages static export, but runbooks still require removed `deploy/status-page/cstate/**` files and Upptime issue flows. | EV-0021; CMD-0024 | F-1211 |
| XFI-0008 | open | Dashboard key form -> dashboard key handler -> platform API key row -> auth subject -> rate-limit middleware | `web/dashboard/src/app/keys/page.tsx`; `internal/api/v1/dashboardkeys/handlers.go`; `internal/platform/postgresstore/apikey_store.go`; `internal/auth/apikey_postgres.go`; `internal/api/v1/middleware/ratelimit.go` | Account tier and billing policy should cap customer-controlled rate limits; runtime enforcement should not accept a self-selected higher paid-tier budget from free accounts. | Free-tier dashboard sessions can request up to 100,000 req/min, the handler stores it, and the runtime middleware honors it as the effective key budget. | EV-0023; CMD-0026 | F-1212 |
| XFI-0009 | open | Stablecoin fiat proxy -> classic USD peg config -> Stellar DEX quote decimals -> min USD volume gate | `configs/example.toml`; `cmd/ratesengine-aggregator/main.go`; `internal/aggregate/stablecoin.go`; `internal/aggregate/orchestrator/orchestrator.go`; `internal/storage/timescale/usd_volume_quote_spec.go`; `internal/storage/timescale/trades.go`; `internal/sources/{sdex,soroswap,phoenix,comet,aquarius}/**` | Fiat-USD VWAP source expansion and the manipulation-resistance `aggregate.min_usd_volume` threshold | When classic/SAC USD-pegged Stellar quotes are treated as fiat-USD contributors, the volume gate must use the same quote decimals as the trade insert USD-volume code, or use the stored `usd_volume` value rather than raw quote amounts. | Proxy expansion includes 7-decimal classic/SAC USDC trades, while `windowUSDVolume` divides every quote amount by `1e8`, undercounting Stellar USD volume by 10x and forcing operators to disable the threshold for R1. | EV-0024; R1-0011 | F-1213 |
| XFI-0010 | open | Local governance policy -> hosted GitHub settings -> release/deploy workflow trust | `CONTRIBUTING.md`; `CODEOWNERS`; `.github/workflows/{ci,release,deploy,docs-deploy,explorer-deploy,status-page}.yml`; `docs/operations/deploy-workflow.md` | GitHub branch protection, rulesets, environment protection, Actions policy, release/deploy permissions | Protected main should enforce reviews, signed commits, required checks, and deployment approvals; workflows should restrict third-party action supply-chain risk. | `main` is unprotected, rulesets/branch protection are not available, deploy environments have no required reviewers, Actions allows all actions and does not require SHA pinning, while workflows execute many tag-pinned third-party actions. | EV-0025; EV-0026 | F-1214; F-1215; F-1216 |
| XFI-0011 | open | SEP-10 challenge generation -> signed challenge verification -> optional replay guard -> JWT issuance -> downstream bearer auth | `cmd/ratesengine-api/main.go`; `internal/auth/sep10/validator.go`; `internal/auth/sep10/redisreplay.go`; `internal/api/v1/auth_sep10.go`; `internal/auth/jwt.go`; `internal/api/v1/middleware/auth.go`; OpenAPI auth docs | A signed SEP-10 challenge should be single-use whenever SEP-10 token issuance is enabled, and startup should fail closed if replay protection cannot be enforced. | Redis replay protection exists and is tested, but the API can still run SEP-10 without Redis/replay guard; guardless deployments preserve replayable signed challenges. R1 currently has SEP-10 disabled. | EV-0027; EV-0053; R1-0012 | F-1217 |
| XFI-0012 | open | Public signup form/OpenAPI -> signup handler -> account/API-key store -> rate-limit middleware | `web/explorer/src/app/signup/page.tsx`; `openapi/rates-engine.v1.yaml`; `internal/api/v1/signup.go`; `internal/auth/signup_tracker.go`; `internal/auth/store.go`; `internal/api/v1/middleware/ratelimit.go` | Anonymous self-service signup should not let attackers cheaply mint many 1000/min keys, and duplicate/idempotency checks must be atomic when relied on. | Signup mints a usable 1000/min key from an unverified email; duplicate tracker is optional; duplicate path is non-atomic and can mint extra keys in races. | EV-0028 | F-1218 |
| XFI-0013 | open | Stripe webhook -> Redis key manager -> platform billing event/audit stores -> dashboard/Postgres keys and subscriptions | `internal/api/v1/stripe_webhook.go`; `cmd/ratesengine-api/main.go`; `internal/auth/*`; `internal/platform/billing.go`; `internal/platform/postgresstore/*`; `migrations/0027_platform_v1_schema.up.sql`; dashboard key UI | Paid upgrades should update the same account/key/subscription source of truth that dashboard users see, with durable webhook idempotency/audit. | Current source has Postgres event dedupe/audit seams, but the upgrade side effect still mutates only Redis keys by signup identifier; subscriptions, Postgres dashboard keys, and account billing state do not change. | EV-0030; EV-0053 | F-1219 |
| XFI-0014 | open | Release artifact -> deploy workflow -> host binaries -> migrations/schema -> rollback docs | `.github/workflows/release.yml`; `.github/workflows/deploy.yml`; `configs/ansible/playbooks/deploy-binary.yml`; `configs/ansible/tasks/deploy-one-binary.yml`; `configs/ansible/roles/archival-node/tasks/14-ratesengine-services.yml`; `migrations/*`; `docs/operations/{deploy-workflow,release-process}.md`; `docs/operations/runbooks/fx-history-missing.md` | A tagged deploy that can change code expecting a new schema must also ship/apply the matching migrations, gate readiness on schema compatibility, and document the actual rollback artifacts operators can use. | Current deploy path swaps binaries only; migration sync/apply is limited to bring-up/manual docs; release docs still claim GHCR image pushes and `/opt/ratesengine/release-<tag>` rollback directories that the current workflows do not create. | EV-0031; EV-0032; R1-0013 | F-1220; F-1221; F-1222 |
| XFI-0015 | open | Source Caddyfile -> deployed Caddyfile -> API remote-IP trust -> public metrics boundary | `configs/caddy/Caddyfile.api`; `configs/caddy/README.md`; `cmd/ratesengine-api/main.go`; `internal/api/v1/middleware/remoteip.go`; R1 `/etc/caddy/Caddyfile`; R1 `/etc/ratesengine.toml` | The deployed edge config should match the reviewed source: Cloudflare client IP should be resolved before forwarding to the API, and `/metrics` should be blocked on the public hostname. | R1 runs an older Caddyfile that forwards `{remote_host}` as XFF, lacks the Cloudflare trusted-proxy block, and exposes `/metrics` publicly with HTTP 200. | EV-0033; R1-0014 | F-1223 |
| XFI-0016 | open | Request middleware remote-IP resolution -> dashboard magic-link/session audit fields -> platform user/token stores | `internal/api/v1/middleware/remoteip.go`; `internal/api/v1/middleware/logger.go`; `internal/api/v1/dashboardauth/handlers.go`; `internal/api/v1/dashboardauth/middleware.go`; `internal/platform/postgresstore/{token_store,user_store}.go`; `migrations/0027_platform_v1_schema.up.sql`; Caddy/API config | Dashboard login, magic-link, and session audit data should record the same resolved client IP that logger/rate-limit/auth use behind a trusted proxy. | Dashboard auth uses `r.RemoteAddr` directly, so proxied production requests record Caddy loopback/proxy IP instead of the customer IP. | EV-0034; R1-0014 | F-1224 |
| XFI-0017 | open | Stablecoin fiat proxy policy -> CAGG history readers -> chart/history API consistency -> R1 historical XLM/USD behavior | `internal/api/v1/history.go`; `internal/api/v1/chart.go`; `internal/api/v1/vwap.go`; `internal/api/v1/ohlc.go`; `internal/api/v1/price.go`; `internal/storage/timescale/aggregates.go`; `[trades].usd_pegged_classic_assets` config; OpenAPI history/chart docs | Every historical USD price surface for Stellar pairs should apply the same declared stablecoin-fiat fallback or clearly mark why a series is unavailable. | `history/since-inception` queries only literal `X/fiat:USD`, while chart and raw-window endpoints retry declared USD pegs; R1 serves chart/direct-USDC history but empty since-inception USD history. | EV-0035; R1-0015 | F-1225 |
| XFI-0018 | open | Dashboard API-key policy fields -> Postgres key store -> runtime auth validator -> rate/quota/permission enforcement | `migrations/0027_platform_v1_schema.up.sql`; `internal/platform/apikey.go`; `internal/platform/postgresstore/apikey_store.go`; `internal/api/v1/dashboardkeys/handlers.go`; `internal/auth/apikey_postgres.go`; `internal/api/v1/middleware/{auth,ratelimit}.go`; OpenAPI/dashboard types | Customer-visible key restrictions and quotas should either be enforced on every authenticated request or hidden/marked pending. | Runtime auth ignores IP allowlists, referer allowlists, permissions, monthly quotas, and last-used updates; only rate-limit override is propagated. | EV-0036 | F-1226 |
| XFI-0019 | open | Docker migration image -> migration binary defaults -> SQL migration directory -> self-hosted schema bootstrap | `docker/ratesengine-migrate.Dockerfile`; `cmd/ratesengine-migrate/main.go`; `migrations/*`; `Makefile`; `docker/README.md`; `configs/ansible/roles/archival-node/tasks/14-ratesengine-services.yml` | A self-hosted migration image should contain the migration files it defaults to applying, or the image/docs/smoke tests should require and verify the expected bind mount/path. | The image copies only the binary; the binary defaults to `migrations`; Docker smoke only checks `--help`; the only complete deploy path is Ansible's separate migration sync to `/usr/local/share/ratesengine/migrations`. | EV-0037 | F-1227 |
| XFI-0020 | open | API HTTP server timeout -> SSE stream handlers -> docs/customer demos -> R1 streaming behavior | `cmd/ratesengine-api/main.go`; `internal/api/streaming/handler.go`; `internal/api/v1/{price_stream,price_tip_stream,observations_stream}.go`; `openapi/rates-engine.v1.yaml`; `docs/operations/{cdn-setup,customer-demo-script}.md`; R1 API service | SSE endpoints should support long-lived client connections with heartbeat writes and reconnect semantics measured in minutes or hours, not an absolute 30s response deadline. | `http.Server.WriteTimeout=30s` sets an absolute response-write timeout per request; R1 tip stream closes at 30s despite 15s heartbeats and 5s events. | EV-0038; R1-0016 | F-1228 |
| XFI-0021 | open | CDN verification script -> API query parameter contract -> streaming cache headers -> operator CDN signoff | `scripts/dev/verify-cdn.sh`; `internal/api/v1/{price,price_tip,price_tip_stream}.go`; `internal/api/streaming/handler.go`; `openapi/rates-engine.v1.yaml` | CDN verification should hit valid representative endpoints and assert the headers those handlers actually promise. | Script probes `base=` on endpoints that require `asset=`, then expects `no-store` on SSE even though the stream handler sets `no-cache`. | EV-0039 | F-1229 |
| XFI-0022 | open | RFP historical retention target -> backfill command -> CAGG history endpoint -> R1 deployed data depth | `docs/freighter-rfp.md`; `docs/architecture/coverage-matrix.md`; `cmd/ratesengine-ops/backfill.go`; `internal/api/v1/history.go`; `internal/storage/timescale/aggregates.go`; R1 Timescale/API | Public historical APIs should meet at least the stated one-year retention target for core Stellar pairs, or expose an explicit completeness/backfill status so clients do not mistake the deployment window for inception. | R1 direct XLM/USDC since-inception daily series starts at 2026-05-03 and returns only nine points; endpoint provides no completeness warning. | EV-0040; R1-0017 | F-1230 |
| XFI-0023 | open | Branch protection assumptions -> canonical CI trigger policy -> direct-main push behavior -> release confidence | `.github/workflows/ci.yml`; `.github/workflows/api-audit.yml`; `CONTRIBUTING.md`; GitHub branch/environment settings | If canonical CI is PR-only, hosted branch protection must enforce PR merges; otherwise main pushes must run the full canonical gate. | `ci` does not run on push to main; `main` is unprotected; path-limited `api-audit` is not equivalent to lint/test/build/security CI. | EV-0041; EV-0025 | F-1231 |
| XFI-0024 | open | Stablecoin catalogue/asset detail price -> `/v1/price` fallback chain -> batch price rows -> customer SDK parity | `internal/api/v1/price.go`; `internal/api/v1/assets.go`; `internal/api/v1/assets_f2.go`; `internal/api/v1/assets_coin_extension.go`; `openapi/rates-engine.v1.yaml`; R1 price/assets endpoints | A canonical stablecoin asset with a displayed USD price on asset detail should also price consistently through single and batch price APIs. | Asset detail has Circle USDC `price_usd`, but `/v1/price` 404s and batch omits it because stablecoin-fiat proxy skips the peg asset itself. | EV-0042; R1-0018 | F-1232 |
| XFI-0025 | open | SDEX legacy XDR claim atoms -> op decoder adapter -> dispatcher metrics -> backfill historical coverage claims | `internal/sources/sdex/decode.go`; `internal/sources/sdex/dispatcher_adapter.go`; `internal/dispatcher/dispatcher.go`; `internal/pipeline/processor.go`; `cmd/ratesengine-ops/backfill.go`; `docs/discovery/protocol-versions.md`; `docs/discovery/dexes-amms/sdex.md`; `internal/sources/sdex/README.md` | Historical replay of classic SDEX should decode every claim-atom variant used in the targeted ledger range, or fail/count visibly when a range is outside supported protocol coverage. | Legacy `ClaimAtomTypeV0` is rejected and then suppressed inside `sdex.Decoder.Decode`, so old fills are dropped without dispatcher decode-error metrics while docs say SDEX backfill goes back to genesis. | EV-0044 | F-1233 |
| XFI-0026 | open | Upstream oracle feed vectors -> canonical asset allow-lists -> decoder partial skips -> dispatcher metrics -> oracle coverage dashboards | `internal/sources/reflector/decode.go`; `internal/sources/redstone/decode.go`; `internal/sources/band/decode.go`; `internal/canonical/asset_*.go`; `internal/dispatcher/dispatcher.go`; `internal/dispatcher/statsflush/flusher.go`; `docs/operations/runbooks/decode-errors.md`; `docs/architecture/explorer-data-inventory.md` | When an upstream oracle starts publishing a feed the product does not yet canonicalize, operators should see an unknown-symbol/feed coverage signal even if other assets in the same event decode successfully. | Reflector, Redstone, and Band skip unknown entries inside mixed batches and return success when any known entry remains, so dispatcher decode-error metrics and decoder stats stay clean while coverage silently lags upstream. | EV-0045 | F-1234 |
| XFI-0027 | open | External websocket frames -> parser errors -> source metrics/runbooks -> source freshness alerts | `internal/sources/external/{binance,kraken,bitstamp,coinbase}/streamer.go`; `internal/sources/external/*/parse.go`; `internal/sources/external/runner.go`; `internal/obs/metrics.go`; `internal/sources/external/README.md`; `docs/operations/runbooks/decode-errors.md`; monitoring rules | Streamer parse/schema failures should be visible as per-source decode or parser-error metrics so vendor schema drift is diagnosable before it becomes only a freshness gap. | Malformed/unknown frames are skipped without metrics in all four CEX stream loops, while the docs say external connectors feed `SourceDecodeErrorsTotal`. | EV-0046 | F-1235 |
| XFI-0028 | open | Ingestion cursors -> supply snapshot ledger -> component at-or-before readers -> asset supply API/market cap | `cmd/ratesengine-aggregator/main.go`; `cmd/ratesengine-ops/supply.go`; `internal/supply/{refresher,lcm_reader,storage_classic_reader,storage_sep41_reader}.go`; `internal/storage/timescale/{account_observations,classic_supply_observations,sep41_supply_events,supply}.go`; asset detail/stat readers | A supply snapshot ledger should represent the ledger through which every required component observer has complete data, or the snapshot should carry component freshness/lag metadata. | Snapshot ledger is max cursor across sources, while component readers use latest-at-or-before values with no freshness bound; stalled observers can produce fresh-ledger snapshots from stale component rows. | EV-0047 | F-1236 |
| XFI-0029 | open | Verified currency IDs -> aggregator pair construction -> CoinMarketCap API query -> stored oracle update identity | `internal/currency/data/seed.yaml`; `internal/currency/verified.go`; `cmd/ratesengine-indexer/main.go`; `internal/sources/external/coinmarketcap/poller.go`; `internal/sources/external/coinmarketcap/poller_test.go` | CMC updates should be keyed to the verified currency's CMC identity when available, not whichever coin CMC returns first for an ambiguous ticker. | Numeric CMC IDs are present but unused; poller queries by `symbol` and takes the first duplicate ticker result. | EV-0048 | F-1237 |

## Required Interaction Classes

| Class | Status | Notes |
| --- | --- | --- |
| binary -> config -> package | in_progress | Core API/indexer/aggregator observed on R1; full per-binary closure still pending |
| config example -> config parser -> deploy template | in_progress | `configs/example.toml`, Ansible, systemd; secret-boundary negative evidence recorded |
| workflow -> script -> artifact | in_progress | CI, OpenAPI generation, web build, dependency audit, release/deploy controls, and hosted GitHub settings reviewed; remaining workflow branches pending |
| Dockerfile -> binary -> runtime flags/env | in_progress | Migration image/default path failure recorded; remaining image env/entrypoint checks pending |
| migration -> Timescale store -> integration test | in_progress | Migration inventory mapped; store/test trace pending |
| decoder -> event type -> pipeline sink -> store | in_progress | SDEX legacy claim-atom/drop path recorded; remaining source decoders pending |
| external adapter -> normalized trade -> aggregation | todo | Every external source |
| aggregator -> Redis/Timescale -> API handler | in_progress | Price/VWAP route and stablecoin proxy reviewed; min-volume decimal mismatch recorded as F-1213 |
| API handler -> OpenAPI -> Go client -> frontend client | in_progress | Removed coins/currencies routes produced concrete drift findings |
| metric -> alert rule -> runbook -> service | in_progress | R1 alerts and missing timer evidence recorded |
| web route -> API hook -> UI state -> SEO/headers | in_progress | Removed endpoint consumers, status probes, dashboard key budget bypass, and signup abuse path recorded |
| docs claim -> code path -> tests/runtime | in_progress | RFP, proposal, ADRs, runbooks; status-page runbook drift recorded |
