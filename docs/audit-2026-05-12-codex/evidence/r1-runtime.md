# R1 Runtime Evidence

R1 runtime checks are allowed by the user. The entries below are
read-only observations captured during audit execution on 2026-05-12.

## Rules

- read-only commands first
- no private keys, tokens, or secret values in evidence
- redact sensitive output
- source review remains mandatory

## Register

| ID | Date | Host | Command | Purpose | Result Summary | Redactions | Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| R1-0001 | 2026-05-12 | `ratesengine-archival-r1` | `hostname; date -u; systemctl status ratesengine-api ratesengine-aggregator ratesengine-indexer` | Confirm core service state | API, aggregator, and indexer were active/running. API uptime was about 10h29m. Recent API logs included `/v1/coins` 200, `/v1/assets/native` 200, and `/v1/markets` 200 with multi-second latencies. Indexer logs showed repeated Coingecko 429 throttling and Kraken reconnects. | No secrets captured | EV-0012; F-1202; F-1208 |
| R1-0002 | 2026-05-12 | `ratesengine-archival-r1` | `systemctl list-timers --no-pager` | Timer inventory | Only `ratesengine-smoke.timer` and `ratesengine-heartbeat@.timer` from the Rates Engine timer family appeared active. SLA, archive-verify, archive-completeness, and supply-snapshot timers were absent from the active timer list. | none | F-1205 |
| R1-0003 | 2026-05-12 | `ratesengine-archival-r1` | `systemctl list-unit-files --no-pager --plain \| grep -E "ratesengine\|sla\|verify\|archive\|supply"` | Installed unit inventory | Installed Rates Engine units were limited to API, aggregator, indexer, heartbeat, and smoke service/timer units. `sla-probe.timer`, `verify-archive-tier-a.timer`, `supply-snapshot.timer`, and `archive-completeness.timer` were not installed. | none | F-1205 |
| R1-0004 | 2026-05-12 | `ratesengine-archival-r1` | `systemctl status ratesengine-sla-probe.timer sla-probe.timer verify-archive-tier-a.timer supply-snapshot.timer archive-completeness.timer` | Confirm missing operational timers | All queried SLA/archive/supply timers returned `Unit ... could not be found.` | none | F-1205 |
| R1-0005 | 2026-05-12 | `ratesengine-archival-r1` | `ss -tlnp` | Listening socket inventory | Public/wildcard listeners included `*:443`, `*:80`, `*:3100` Loki, `*:6061` Galexie admin, `*:9000` MinIO, `*:9001` MinIO console, `*:9080` Promtail, `*:9090` Prometheus, `*:9100` node_exporter, `*:9096` Loki grpc, plus `0.0.0.0:11726`. Loopback listeners included API/Postgres/Redis and metrics ports. | PIDs/service names retained; no secret values | F-1201 |
| R1-0006 | 2026-05-12 | `ratesengine-archival-r1` | `nft list ruleset; systemctl status nftables ufw; ufw status verbose` | Firewall posture | `nft list ruleset` returned an empty ruleset; `nftables.service` was disabled and inactive; `ufw.service` was enabled but `ufw status verbose` reported `Status: inactive`. | none | F-1201 |
| R1-0007 | 2026-05-12 | `ratesengine-archival-r1` | `df -h; free -h` | Host capacity snapshot | `/var/lib/minio` was 6.4T total, 4.9T used, 78% full. Memory was 188Gi total, 179Gi used, 8.4Gi available. Swap was 4.0Gi total, 4.0Gi used. | none | F-1209 |
| R1-0008 | 2026-05-12 | external workstation to `136.243.90.96` | `nc -vz -w2 ...` | External reachability proof | TCP connects succeeded externally to ports 9000, 9001, 9090, 3100, 9080, 9100, 6061, and 11726. | none | EV-0019; F-1201 |
| R1-0009 | 2026-05-12 | `ratesengine-archival-r1` | `curl http://localhost:3000/v1/healthz`; `curl http://localhost:3000/v1/readyz` | API health/ready truth | Health returned `ok`; readiness returned `ok` with postgres/redis true, despite R1 having missing timers, public infra ports, source-stopped alerts, p99 latency alert, and memory alert. | none | F-1210 |
| R1-0010 | 2026-05-12 | `ratesengine-archival-r1` | `curl -sS http://localhost:9090/api/v1/alerts` | Alert truth | Firing alerts included `ratesengine_ingestion_source_stopped` for `ecb`, `soroswap`, `band`, `phoenix`; pending alerts included `comet`, `blend`, `redstone`; `ratesengine_api_latency_p99_high` fired with API p99 about 3.325s; `ratesengine_host_memory_high` fired at about 95.41%. Deadmansswitch was firing as designed. | Alert payload only; no secrets | F-1208; F-1209; F-1210 |
| R1-0011 | 2026-05-12 | `ratesengine-archival-r1` | `grep -nE "enable_stablecoin_fiat_proxy|min_usd_volume|usd_pegged_classic_assets" /etc/ratesengine.toml`; `sed -n '44,62p' /etc/ratesengine.toml` | Non-secret aggregator/trades config check | R1 has `[aggregate] enable_stablecoin_fiat_proxy = true`, `min_usd_volume = 0`, and `[trades] usd_pegged_classic_assets` containing Circle USDC. The config comment says the default is 10000 but R1 disables it because on-chain micro-volume XLM/USDC trades are current data. | no secret values present in the selected lines | F-1213 |
| R1-0012 | 2026-05-12 | `ratesengine-archival-r1` | `curl http://127.0.0.1:3000/v1/auth/sep10/challenge?...` | SEP-10 runtime configuration | R1 returned 503 with problem type `https://api.ratesengine.net/errors/sep10-unavailable` and title `SEP-10 not configured`; no signing seed/validator is wired on R1. | request account was a public G-address test value; no secrets | EV-0027 |
| R1-0013 | 2026-05-12 | `ratesengine-archival-r1` | `sudo -u postgres psql -d ratesengine -tA -c "SELECT version, dirty FROM schema_migrations ORDER BY version DESC LIMIT 1"; ls /usr/local/share/ratesengine/migrations; cat /var/lib/ratesengine/deployed-versions/*` | Schema/deployed-version coupling | Database schema reported `28|f`. Deployed-version sidecars reported API, aggregator, and indexer at `v0.5.0-rc.47`; ops at `v0.5.0-rc.29`; sla-probe at `v0.5.0-rc.17`. `/usr/local/share/ratesengine/migrations` produced no migration-file listing in this probe. | no DSN or secret values printed | EV-0031; F-1220 |
| R1-0014 | 2026-05-12 | `ratesengine-archival-r1` and external workstation | `grep ... /etc/ratesengine.toml`; `sed -n '1,135p' /etc/caddy/Caddyfile`; `curl -skI https://api.ratesengine.net/metrics`; `curl -sk https://api.ratesengine.net/metrics` | Live Caddy/API request-boundary posture | R1 API listens on `127.0.0.1:3000`, trusts proxy CIDR `127.0.0.1/32`, and uses `auth_mode=apikey_optional`. Live Caddy forwards `X-Forwarded-For {remote_host}` and has no Cloudflare trusted-proxy block or `/metrics` guard. Public `/metrics` returned HTTP 200 and Prometheus metric text. | Cloudflare API token reference appears as an env placeholder only; no token value captured | EV-0033; F-1223; F-1224 |
| R1-0015 | 2026-05-12 | external workstation | `curl -sk 'https://api.ratesengine.net/v1/history/since-inception?...'`; `curl -sk 'https://api.ratesengine.net/v1/chart?...'` | Historical XLM/USD fallback behavior | `native/fiat:USD` since-inception history returned `points: []`; `native/fiat:USD` chart for 1w/1d returned six populated daily points with `flags.triangulated=true`; direct `native/USDC-GA5ZSEJY...` since-inception history returned nine populated daily points. | no secrets | EV-0035; F-1225 |
| R1-0016 | 2026-05-12 | `ratesengine-archival-r1` | `timeout 45 curl -sN --max-time 42 http://127.0.0.1:3000/v1/price/tip/stream?asset=native&quote=fiat:USD ...` | Live SSE connection lifetime | The loopback stream emitted `:connected` and `tip_update` at elapsed 0s, then `tip_update` at 5s, 10s, 15s, 20s, and 25s, with a `:keepalive` at 15s. The stream ended at `elapsed 30`, matching the API server's 30s write timeout. | no secrets | EV-0038; F-1228 |
| R1-0017 | 2026-05-12 | `ratesengine-archival-r1` | `curl http://127.0.0.1:3000/v1/history/since-inception?asset=native&quote=USDC-GA5Z...&granularity=1d | jq ...` | Direct since-inception coverage depth | The direct XLM/Circle-USDC daily history returned `count=9`, first point `2026-05-03T00:00:00Z`, last point `2026-05-11T00:00:00Z`, and `as_of=2026-05-12T14:09:49Z`. | no secrets | EV-0040; F-1230 |
| R1-0018 | 2026-05-12 | `ratesengine-archival-r1` | `curl /v1/price?...USDC...`; `curl /v1/price/batch?...USDC...`; `curl /v1/assets/USDC-GA5Z...` | Stablecoin USD price parity | Asset detail returned `price_usd=0.9999838427` for Circle USDC. Single `/v1/price` for the same asset/quote returned 404. GET and POST batch returned empty `data:[]`. | no secrets | EV-0042; F-1232 |
| R1-0019 | 2026-05-12 | external workstation + `ratesengine-archival-r1` | `curl -sk 'https://api.ratesengine.net/v1/history/since-inception?asset=native&quote=fiat:USD&granularity=1d'`; `curl -sk 'https://api.ratesengine.net/v1/history/since-inception?asset=native&quote=USDC-GA5ZSEJY...&granularity=1d'`; `sed -n '52,64p' /etc/ratesengine.toml` | Refreshed XLM/USD since-inception fallback truth | Public `native/fiat:USD` still returned `points: []`. Direct `native/USDC-GA5ZSEJY...` returned nine populated daily points. Live config still has `enable_stablecoin_fiat_proxy = true` and `[trades].usd_pegged_classic_assets` containing Circle USDC. | no secrets | EV-0140; F-1225 |
| R1-0020 | 2026-05-12 | `ratesengine-archival-r1` | loopback Python wrapper around `curl -sN --http1.1 --max-time 68 'http://127.0.0.1:3000/v1/price/tip/stream?asset=native&quote=fiat:USD&window_seconds=60'` | Refreshed SSE lifetime proof after the source-side write-deadline reset landed | The stream emitted `:connected`, one initial `tip_update`, and a `:keepalive` at 15.0s, then terminated at elapsed `30.0` seconds with curl exit 18 despite a 68-second client ceiling. | no secrets | EV-0141; F-1228 |
