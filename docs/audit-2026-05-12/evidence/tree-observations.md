# Tree Observations

Tree-shape observations that don't fit the row-per-finding ledger.

| ID | Observation | Evidence | Notes |
| --- | --- | --- | --- |
| T-1201 | Repo grew materially since 05-02: 1747 tracked files (vs 1223), 711 Go files (vs 567), 327 test files (vs 275). Net additions live primarily in `internal/aggregate/{anomaly,baseline,changesummary,confidence,freeze,orchestrator}`, `internal/api/streampublish`, `internal/canonical/discovery`, `internal/currency/data`, `internal/dispatcher/statsflush`, `internal/incidents`, `internal/notify`, `internal/platform`, `internal/sources/{forex,frankfurter}`, `internal/sources/external/{exchangeratesapi,polygonforex}`, `internal/usage`, `internal/auth/sep10`. Migration count grew from 30 to 56 (28 up + 28 down). Workflow count grew from 3 to 10. Web frontends grew from 1 (explorer) to 3 (explorer + dashboard + status). Runbook count grew from ~50 to 70. | inventory/repo-snapshot.md, inventory/area-counts.md | All net-new code is in scope by default; flag any package that is wired but untested |
| T-1202 | Pre-built binaries committed in worktree (`ratesengine-api`, `ratesengine-aggregator`, `ratesengine-indexer`) but `.gitignore` claims they should be ignored. Verify with `git ls-files` whether they are tracked or just present. | `ls -la` at repo root | If tracked: finding (binary residue + supply-chain risk). If untracked: cosmetic, but still a low finding for a clean checkout |
| T-1203 | `.discovery-repos/` contains 29 cloned upstream projects (aquarius-amm, band-soroban, blend-contracts, cdp-pipeline-workflow, comet-contracts, flowctl, go-stellar-sdk, nebu, nebu-sql, obsrvr-bronze-copier, obsrvr-lake-writer, orbit-contracts, phoenix-contracts, redstone-public-contracts, redstone-rust-sdk, reflector-contract, rs-stellar-archivist, script3-sep40, soroswap-core, stellar-docs, stellar-etl, stellar-extract, stellar-galexie, stellar-ledger-data-indexer, stellar-protocol, stellar-rpc). Confirm none are imported by product code (W04). | `find .discovery-repos -maxdepth 1 -type d` | Excluded by EX-1204 from per-file walk; W04 covers import-leak check |
| T-1204 | `.wrangler/` directory exists at repo root — Cloudflare Workers / Pages local dev cache. Verify `.gitignore` excludes it cleanly. | `ls -la` at repo root | Likely benign but confirm no secrets in cache |
| T-1205 | `web/{explorer,dashboard,status}/out/` directories likely contain checked-in static export artifacts. Verify (could be ignored per `.gitignore`). | `git ls-files web/explorer/out/` | If tracked: low finding (build output bloat). EX-1212 excludes from walk |
| T-1206 | ADR numbering jumps from 0011 to 0013 (no `0012-*.md` in `docs/adr/`). Either deliberately skipped or quietly removed. | inventory/adr-inventory.md, `ls docs/adr/` | Investigate: was 0012 superseded? deleted? renumbered? |
| T-1207 | `internal/sources/forex/circulation_data.csv` — fiat circulating supply data shipped as repo-local CSV embedded in binary. Updating supply = redeploy. | internal/sources/forex/circulation.go | W12 — confirm embed path + freshness policy |
| T-1208 | `openapi/rates-engine.v1.yaml` is 165 KB single file. Per-route lookup will require careful navigation. | inventory/api-route-inventory.md | Audit budget: every route must be traced through this single file |
| T-1209 | `migrations/0027_platform_v1_schema.up.sql` is 19 KB — the single largest migration; introduces the entire platform schema. Per-statement audit required. | inventory/migration-inventory.md | High-risk surface; covers billing/auth/audit |
| T-1210 | 70 runbooks vs 64 alert files in `deploy/monitoring/rules/`. Some runbooks may exist without firing alerts (informational), some alerts may lack runbooks. | inventory/runbook-inventory.md, inventory/alert-rule-inventory.md | R07 reconciliation must catch both gaps |
| T-1211 | `cmd/ratesengine-aggregator/main_test.go` is the only `_test.go` file under `cmd/` for any binary except api/indexer. Most binaries have tests; verify each `cmd/<binary>/main.go` has at least one `main_test.go`. | inventory/file-coverage.tsv | Test gap finding for any binary lacking entry-point tests |
| T-1212 | `internal/sources/external/coingecko/` has only 5 files (poller, identity, decimal_helpers, backoff, tests). The poller is the source of divergence-reference data; ensure it is robust. | internal/sources/external/coingecko/ | Limited surface is fine if it covers the contract; verify in W08 |

Add new tree observations as the audit walks the workspace.
