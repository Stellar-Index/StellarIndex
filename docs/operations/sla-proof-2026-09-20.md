---
title: SLA proof report — 2026-09-20
status: evidence (generated — do not hand-edit)
generator: scripts/ops/sla-proof-from-probe.sh
related:
  - docs/adr/0009-latency-budget.md
  - docs/operations/sla-proof-procedure.md
  - cmd/stellarindex-sla-probe/main.go
---

# SLA proof report — 2026-09-20

**Verdict: NOT PROVEN.**

Generated by
[`scripts/ops/sla-proof-from-probe.sh`](../../scripts/ops/sla-proof-from-probe.sh)
by aggregating the `stellarindex_sla_probe_*` series that
`stellarindex-sla-probe` writes into node_exporter's textfile collector
every 15 minutes. Every number below is a literal reading of those
series; nothing is hand-entered, and the generator refuses to write a
report at all when a headline series is absent, when the run cannot name
its own provenance, or when the retained window is too short to carry a
claim.

## Read this before reading the numbers

Four things this measurement is not, each of which would otherwise be
assumed from the shape of the document:

1. **It is not a load test.** The probe drives 1 concurrent worker(s)
   in ~30 s bursts every 15 minutes; it measures served latency under
   whatever real traffic the host had at the time. ADR-0009 states
   p95 ≤ 200 ms and p99 ≤ 500 ms **unconditionally** — the target is not
   qualified by a request rate — so a low-concurrency measurement does
   test the stated target. What it does **not** exercise is ADR-0009's
   *enforcement* clause, which calls for a load test at volume. Nothing
   here is evidence about behaviour under concurrency the probe never
   applied.
2. **The probe measures `http://localhost:3000/v1` from inside the box.** Its requests never
   traverse DNS, TLS, the reverse proxy or any CDN, so the numbers
   below EXCLUDE every component ADR-0009's budget allocates outside
   the API process — 5 ms of TLS terminate + proxy ingress and 15 ms of
   network egress in the p95 table alone. **A client's p95 is strictly
   larger than every latency figure in this report.**
3. **Percentiles were not averaged, because percentiles do not average.**
   The probe exports one p50/p95/p99 per run and never the underlying
   samples, so the request-level population percentile for this window is
   not recoverable and is not computed here. The headline column is the
   **largest per-run percentile in the window**, which is a true upper
   bound on the pooled one: every run has ≥ 95 % of its samples at or
   below its own p95, so pooling runs keeps ≥ 95 % at or below the largest
   of those values, for any run sizes. The bound runs one way — under the
   target it PROVES the target held; over the target it proves only that
   this bound cannot settle it.
4. **Nothing is rolled up across endpoints.** There is no "overall p95"
   in this document. The verdict is the conjunction of the per-endpoint
   bounds, not a percentile of a merged population.

## Provenance

| Field | Value |
| --- | --- |
| Claim under test | ADR-0009: p95 ≤ 200 ms, p99 ≤ 500 ms; probe availability ≥ 99.9 % |
| Method | aggregate of `stellarindex_sla_probe_*` gauges; per-run percentiles bounded, never averaged |
| Instrument | `stellarindex-sla-probe`, systemd timer `stellarindex-sla-probe.timer` (15 min cadence) |
| Probe target | `http://localhost:3000/v1` |
| Probe concurrency | 1 |
| Probe host | `r1` |
| Metrics source | `http://127.0.0.1:19090`, `node_exporter textfile collector` |
| Window requested | `7d` |
| Window measured (UTC) | `2026-09-13T07:37:26Z` → `2026-09-20T07:37:14Z` (6d 23h) |
| Commit at render time | `8078fdaec0230f8cb557faca7822ade51e5f1258` |
| API builds observed in window | 15 — `v0.79.0`, `v0.80.0`, `v0.81.0`, `v0.82.0`, `v0.83.0`, `v0.84.0`, `v0.85.0`, `v0.86.0`, `v0.87.0`, `v0.88.1`, `v0.89.0`, `v0.89.1`, `v0.89.3`, `v0.90.0`, `v0.91.0` |
| CI run | https://github.com/Stellar-Index/StellarIndex/actions/runs/35497357526 |
| Extract | read live from http://127.0.0.1:19090, sha256 `d57da8a597d90d56631d2b210f9ecae59a4d37800f71664a5d6378bf4814c4d6` |

## Window and coverage

A window with holes in it is not a clean window, and averaging over
what survived would hide exactly the minutes worth looking at. Both
kinds of hole are measured, because they are different failures: the
textfile collector keeps re-serving the last file the probe wrote, so a
dead probe leaves a perfectly continuous series.

| Coverage question | Measured |
| --- | --- |
| Scrapes of the probe series in the window | 40320 of ~40319 expected (100.00 %) |
| Scrape interval (from the last hour) | 15 s |
| Holes in the series (scraping stopped) | none |
| Passing probe runs recorded | 617 |
| Longest stretch with no recorded passing run | 3h 20m |
| Window spent under the probe's own FAIL verdict | 3.39 % |
| Probe run duration (min/max) | 30.0 s / 30.0 s |

The window is continuous at the configured floor (coverage ≥ 95 %, no
holes), so the aggregates below cover it rather than a surviving
fraction of it.

## Result — per endpoint

`worst-run p95` / `worst-run p99` are the largest value any single probe
run produced in the window — the bound described above. `samples/run` is
the probe's own request count for that endpoint.

| Endpoint | samples/run | worst-run p95 | ≤ 200 ms | worst-run p99 | ≤ 500 ms | window availability | ≥ 99.9 % | worst run |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `assets` | 1073 | 59.0 ms | PROVEN | 7621.8 ms | NOT PROVEN | 97.525 % | NOT PROVEN | 33.140 % |
| `diagnostics-cursors` | 1073 | 17.0 ms | PROVEN | 41.2 ms | PROVEN | 97.526 % | NOT PROVEN | 33.148 % |
| `healthz` | 1073 | 0.0 ms | PROVEN | 2.0 ms | PROVEN | 100.000 % | PROVEN | 100.000 % |
| `issuers` | 1073 | 2.0 ms | PROVEN | 12.2 ms | PROVEN | 97.526 % | NOT PROVEN | 33.140 % |
| `markets` | 1073 | 2.0 ms | PROVEN | 9.7 ms | PROVEN | 97.526 % | NOT PROVEN | 33.148 % |
| `oracle-latest` | 1072 | 2.0 ms | PROVEN | 8.0 ms | PROVEN | 97.523 % | NOT PROVEN | 33.125 % |
| `price` | 1072 | 111.3 ms | PROVEN | 257.3 ms | PROVEN | 97.522 % | NOT PROVEN | 33.125 % |
| `price-tip` | 1072 | 22.8 ms | PROVEN | 64.8 ms | PROVEN | 97.523 % | NOT PROVEN | 33.125 % |
| `readyz` | 1073 | 4.6 ms | PROVEN | 12.1 ms | PROVEN | 100.000 % | PROVEN | 100.000 % |
| `version` | 1073 | 0.0 ms | PROVEN | 2.0 ms | PROVEN | 100.000 % | PROVEN | 100.000 % |

The two latency columns are **bounds**; the availability column is an
**estimate**, and they are different kinds of number on purpose. A
percentile cannot be pooled across runs, so the largest per-run value is
published instead and is a true ceiling on the pooled one. Availability
is a ratio and the SLO is a ratio over the window, so gating it on the
worst single 30-second run would fail a window the SLO was met in — it is
weighted by each run's own sample count instead. `worst run` is kept
beside it because a window ratio hides a short total outage, and the
`window below target` column further down says how much of the week that
was.

Sample size: **1073 per endpoint per run** on average (min 125, max 4312 over
the window), across at least **617 recorded passing runs** — on the order of
**6,618,645 requests across all 10 endpoints** over 6d 23h. That is a real n, and it
is also 6,618,645 requests issued by one process from one vantage point, which is
not the same evidence as the same count spread across real clients. Run
sizes are not uniform, which is why nothing here is a simple average of
per-run values.

### Typical run — descriptive only

The median of the per-run percentiles. This is a description of **the
runs**, not a percentile of requests, and it is time-weighted: each run's
value is held in the series until the next run overwrites it, and the
timer carries up to 60 s of randomised delay, so runs are not weighted
exactly equally. Read it as "what a normal run looked like"; do not
quote it as a latency percentile.

| Endpoint | worst-run p50 | median-run p95 | median-run p99 | best-run p95 | window over p95 target | window over p99 target | window below availability target |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `assets` | 19.0 ms | 9.0 ms | 17.9 ms | 4.0 ms | 0.000 % | 0.149 % | 3.239 % |
| `diagnostics-cursors` | 5.0 ms | 4.0 ms | 7.0 ms | 2.0 ms | 0.000 % | 0.000 % | 3.239 % |
| `healthz` | 0.0 ms | 0.0 ms | 0.0 ms | 0.0 ms | 0.000 % | 0.000 % | 0.000 % |
| `issuers` | 0.0 ms | 0.0 ms | 2.0 ms | 0.0 ms | 0.000 % | 0.000 % | 3.239 % |
| `markets` | 1.0 ms | 0.0 ms | 2.0 ms | 0.0 ms | 0.000 % | 0.000 % | 3.239 % |
| `oracle-latest` | 0.0 ms | 0.0 ms | 2.0 ms | 0.0 ms | 0.000 % | 0.000 % | 3.239 % |
| `price` | 35.0 ms | 16.0 ms | 26.0 ms | 9.0 ms | 0.000 % | 0.000 % | 3.239 % |
| `price-tip` | 10.0 ms | 3.4 ms | 7.0 ms | 1.0 ms | 0.000 % | 0.000 % | 3.239 % |
| `readyz` | 0.0 ms | 0.0 ms | 2.0 ms | 0.0 ms | 0.000 % | 0.000 % | 0.000 % |
| `version` | 0.0 ms | 0.0 ms | 0.0 ms | 0.0 ms | 0.000 % | 0.000 % | 0.000 % |

`worst-run p50` is the largest per-run p50 in the window. It sits in the
descriptive table rather than the headline one because ADR-0009 makes no
median claim. `window over … target` is the fraction of the window during
which the most recent run's percentile was above the target — a
time-weighted reading of how much of the week a breach was the standing
state, not a count of requests.

### Freshness — reported, not gated

Only endpoints that return an `observed_at` carry this series.
`/price` serves the ADR-0015 closed bucket and is structurally 30–150 s
old by design; the 30 s pricing SLA is served and measured on
`/price/tip`.

These rows carry no verdict, and that is deliberate. The deployed
freshness bound is enforced by `stellarindex_sla_probe_freshness_breach`
in `deploy/monitoring/rules/sla-probe.yml`, which requires the breach to
be **sustained for 30 minutes** — because a single run over bound is
in-contract: `computeTip` escalates its window and falls back to the
closed bucket, so a pair with no recent trade legitimately serves a
60–120 s `observed_at` (ADR-0018). Gating a worst-single-run value
against a threshold the live alert deliberately does not gate that way
would invent a stricter bar than the system asserts.

| Endpoint | stated bound | worst-run freshness | median-run freshness |
| --- | --- | --- | --- |
| `price` | ≤ 150 s | 106.7 s | 76.7 s |
| `price-tip` | ≤ 30 s | 103.5 s | 0.0 s |

## What this report does and does not prove

- It proves a **bound**, not a value. Where a row reads PROVEN the
  pooled percentile for that endpoint over this window cannot have
  exceeded the target. Where it reads NOT PROVEN the pooled percentile
  may still have been inside the target — the bound simply cannot settle
  it, and the per-run breach that produced it is named in the
  `window over target` column above.
- It excludes the **edge**. DNS, TLS terminate, the reverse proxy and
  any CDN are outside the measured path entirely. ADR-0009 defines its
  budget as edge-to-client and allocates 5 ms + 15 ms to exactly those
  hops, so a client-observed p95 is strictly larger than every figure
  here and this report does not evidence the end-to-end claim.
- It says nothing about **behaviour under load**. `test/load/scenarios/`
  holds the k6 scenarios that would, including the canonical 300 rps ×
  10 min soak; they compile on every PR and are one `workflow_dispatch`
  away from running, and they have no production-shaped target to run
  against. See docs/operations/sla-proof-procedure.md.
- It gates every endpoint at the headline 200 / 500 ms. ADR-0009's
  per-endpoint table is **tighter** for `/healthz`, `/readyz` and
  `/version` (p95 ≤ 5 ms, p99 ≤ 20 ms) and states no budget at all for
  `/assets`, `/issuers`, `/markets`, `/diagnostics/cursors` or
  `/price/tip`. A PROVEN row for a smoke endpoint here is therefore not
  a test of its tighter budget.
- The availability figure is the probe's own 2xx rate over its own
  requests. It is not an uptime SLO: a window in which the probe was
  the only client is not a window in which users were served.
- It is **not a measurement of one build.** 15 API builds were live
  inside this window (`v0.79.0` … `v0.91.0`), so the numbers describe a moving
  target and every deploy restart inside the window is in them. "Commit
  at render time" names where the tree was when this document was
  generated, not a single build the whole window ran.

## Notes / caveats

Rendered by the scheduled sla-proof-weekly run from a read-only port-forward to the probe host's Prometheus. The probe target recorded above came from the documented default in configs/healthchecks/sla-probe.sh, not from a value read off the host.

## Reproduce

```sh
# On the probe host, or through a port-forward to its Prometheus.
SLA_PROOF_PROBE_TARGET='http://localhost:3000/v1' \
  SLA_PROOF_PROBE_CONCURRENCY='1' \
  SLA_PROOF_COMMIT="$(git rev-parse HEAD)" \
  scripts/ops/sla-proof-from-probe.sh \
    --prom-url http://127.0.0.1:19090 \
    --window 7d \
    --extract-out sla-probe-extract.json
```

The extract is the exact set of query results this report was rendered
from, including the promql of every query. Re-rendering it with
`--extract sla-probe-extract.json` reproduces every number in this
document with no network access at all; the only line that differs is the
`Extract` row above, which records that the second pass was a re-render.
