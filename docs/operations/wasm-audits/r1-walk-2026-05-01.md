---
title: r1 WASM walk — comprehensive history (2026-05-01)
last_verified: 2026-05-01
status: ratified for sources with `BackfillSafe=true`; Blend pending
related:
  - docs/operations/r1-deployment-state.md
  - docs/operations/wasm-audits/README.md
  - internal/sources/external/registry.go (BackfillSafe flags)
---

# r1 WASM walk — comprehensive history

> Snapshot of the actual WASM hash timeline for every Soroban
> source we ingest. Built from r1's full Galexie archive walk
> (`s3://galexie-archive`, ledgers 50,457,424 → 62,296,694)
> captured 2026-04-30, supplemented by Soroban-RPC current-state
> queries for never-upgraded contracts the walker missed.
>
> **Coverage: 540 / 543 contracts attributed across 8 sources
> (99.4 %)**, including all 11 Blend contracts. All 52 unique
> WASMs fetched + SHA-256-verified + disassembled + classified
> by exported function signatures.

## What this answers

Per protocol/oracle that produces canonical trades:

1. **Which contracts are deployed on mainnet** — every factory,
   router, per-instance pool, and oracle contract.
2. **When each was deployed** — first-seen ledger from the walk,
   or "pre-walk-window" for contracts on never-upgraded WASMs.
3. **When each was upgraded** — every
   `update_current_contract_wasm` event captured as a contiguous
   ledger range per active WASM hash.
4. **What each upgrade resulted in** — factory upgrades cascade
   onto pool instances deployed afterwards; existing instances
   stay on their old WASM until individually upgraded.
5. **What each WASM does** — function exports + symbol strings
   from disassembly, classified into roles
   (`<source>-{factory,router,pool,multihop,oracle,...}`).

## Top-level numbers

| Metric | Count |
|---|---|
| Sources covered | 8 (Soroswap, Aquarius, Phoenix, Reflector, Comet, Redstone, Band, Blend) |
| Contracts attributed (incl. Blend) | 540 / 543 |
| Contracts with observed transitions in walk | 249 |
| Contracts resolved via RPC current-state | 291 (280 walker-silent + 11 Blend) |
| Contracts unresolved (TTL-evicted instance storage) | 3 |
| Unique WASM hashes | 52 (47 walk + 3 Aquarius never-upgraded + 2 Blend non-pool roles) |
| WASMs fetched + SHA-256-verified bytes preserved on disk | 52 / 52 |
| Total ledger ranges (hash periods) across all contracts | ~1,200 |

## Per-source picture

### Soroswap — `internal/sources/soroswap/`

**196 contracts, 3 unique WASMs, role breakdown:** 194 pairs + 1 factory + 1 router.

| Role | Count | WASM | Stable? |
|---|---|---|---|
| Factory | 1 | `5db738b05d9148128a240b0e2c1cb935c2805192bf98a579421aacda364c8dae` | yes — no upgrades observed |
| Router | 1 | `4c3db3ebd2d6a2ab23de1f622eaabb39501539b4611b68622ec4e47f76c4ba07` | yes |
| Pair | 194 | `18051456816b66f12e773a56f77c5794fac1b1fb7ab6e22d4fad5a412770f73e` | yes — every pair runs the same WASM |

**Reading:** Soroswap's WASMs have been **completely stable** since deployment. The factory has never upgraded (`update_current_contract_wasm` never fired), the router likewise, and every pair instance runs the single canonical pair WASM. From a backfill-safety perspective this is the simplest source we have.

### Aquarius — `internal/sources/aquarius/`

**314 contracts, 19 unique WASMs**, role breakdown: 313 pools + 1 router. **(Matches the audit doc's "313 pools" exactly.)**

WASM diversity is split across two cohorts:

**Cohort A — never-upgraded pools** (resolved via RPC current-state, since the walker only emits transitions and these never transitioned):
- `ae0da5a84b15805c…` — **149 pools** (volatile / ConstantProductLiquidityPool)
- `f1077e0b77da5e62…` — 13 pools (StableswapLiquidityPool)
- `8875f0c770fb26d3…` — 6 pools (rewards-enhanced variant)

These are exactly the three hashes the original `aquarius.md` audit-doc cites — they were always correct, just invisible to a transitions-only walker. Disassembly confirms all three expose the Aquarius pool API (`deposit`, `estimate_swap`, `get_reserves`, `get_pools_plane`, `get_rewards_info`).

**Cohort B — upgraded pools** (5-WASM upgrade chain captured in walk transitions):

| Order | WASM | Pool count | Observed in ledger range |
|---|---|---|---|
| 1 (oldest) | `b54ba37b…` | 97 | 52.7M+ |
| 2 | `2d770946…` | 70 | upgraded mid-2024 |
| 3 | `7cecf23b…` | 36 | further iteration |
| 4 | `a1629dcd…` | 118 (most-current as of 2026-04-30) | 58M+ |
| 5 (newest variant) | `4f080d24…` | 18 (rolling out) | 58M+ |

**Aquarius router (`CAVLP5DH…`)** is in the input list but its instance entry was TTL-evicted at RPC query time; the walk never observed a transition either. The router is operationally live (we ingest events through it daily). To capture its WASM hash we'd need either (a) to extend the entry's TTL via an invocation and re-query, or (b) to walk the archive for the contract's deploy ledger entry. Tracked under "remaining gaps" below.

### Phoenix — `internal/sources/phoenix/`

**13 contracts, 22 unique WASMs**, role breakdown: 11 pools + 1 factory + 1 multihop. **(Matches audit doc "11 pools".)**

This is the **most-iterated source** in the walk:
- 5 distinct factory WASM versions
- 3 distinct multihop WASM versions
- 14 distinct pool WASM versions

The factory upgrades cascaded into pool template upgrades — operators iterated the LP API more frequently than soroswap or aquarius. Each pool WASM exposes the Phoenix pool API (`provide_liquidity`, `withdraw_liquidity`, `simulate_swap`, `query_pool_info`).

The audit doc's two cited pool hashes (`13b15865…`, `167ab414…`) are confirmed in the walk and are **two of the 14 pool variants** — the audit captured a snapshot of two specific versions, not the full upgrade chain.

### Reflector — `internal/sources/reflector/{dex,cex,fx}`

**3 contracts, 2 unique WASMs.**

| Oracle | Contract | First-seen ledger |
|---|---|---|
| DEX | `CALI2BYU…` | 50,644,229 |
| CEX | `CAFJZQWS…` | 50,644,239 |
| FX | `CBKGPWGK…` | similar |

All three migrated v2 (`4a64c8c8…`) → v3 (`df88820e…`) coherently. Disassembly of both versions exposes the SEP-40 oracle API (`lastprice`, `prices`, `price`, `base`, `assets`, `decimals`, `resolution`). The audit-doc citations match the walk exactly.

### Comet — `internal/sources/comet/`

**1 contract, 1 WASM.**

The Blend-backstop pool `CAS3FL6T…` runs WASM `8abc28913035c074…`, stable since deployment. Disassembly confirms Balancer v1 / Comet API (`swap_exact_amount_in`, `swap_exact_amount_out`, `join_pool`, `exit_pool`, `get_normalized_weight`).

### Redstone — `internal/sources/redstone/`

**1 contract, 2 unique WASMs.**

The adapter `CA526Y2N…` ran:
- `b400f7a8…` — hotfix, **35-minute lifetime** (ledger 58,758,722 → 58,759,141)
- `5e93d22c…` — production, in place since

The 419-ledger gap is the documented "first deploy went sideways, hotfix landed minutes later" event. Disassembly of both shows the same `write_prices`/`read_prices`/`read_timestamp` API.

**Pre-backfill safety check** (any `ratesengine-ops backfill` overlapping the hotfix window):

```sql
SELECT count(*) FROM trades
 WHERE source = 'redstone'
   AND ledger BETWEEN 58758722 AND 58759141;
-- Must return 0 before backfill replay covering this range.
```

### Band — `internal/sources/band/`

**1 contract, 1 WASM.**

`CCQXWMZV…` runs `6cdb9a3cdeec01a1…`, stable since deployment at ledger 50,842,736. Disassembly confirms BandChain StandardReference API (`get_reference_data`, `add_relayers`, `force_relay`, `relay`).

### Blend — `internal/sources/blend/`

**11 contracts, 3 unique WASMs**, role breakdown: 9 pools + 1 backstop + 1 pool factory.

Resolved via Soroban-RPC current-state queries (the targeted wasm-history walk took >1h without producing buffered output, so we pivoted to the same RPC-based approach that worked for the 280 walker-silent contracts in §Top-level numbers).

| Role | Count | WASM | Notes |
|---|---|---|---|
| Lending pool | 9 | `a41fc53d6753b6c04eb15b021c55052366a4c8e0e21bc72700f461264ec1350e` | Phase 1 audit cohort. Bytes preserved at `evidence/blend/pool-a41fc53d6753b6c0.wasm` from earlier audit; SHA-256-confirmed via this audit |
| Backstop module | 1 (`CAQQR5SW…`) | `c1f4502a757e25c611f5a159bc1ab0eef64085adac6c68123dca66e87faffbc2` | Exports `gulp_emissions`, `reward_zone`, `add_reward`, `backstop_token` — Blend backstop API |
| Pool Factory | 1 (`CDSYOAVX…`) | `31328050548831f63d2b72e37bcfd0bb7371b7907135755dbe09ed434d755ca9` | Tiny (3.2 KB); exports `deploy`, `is_pool` — registry only |

**Reading:** the audit doc enumerated 11 contracts as "Blend pools" but the on-chain reality is 9 actual lending pools + 1 backstop + 1 pool-factory. Each role runs a distinct WASM with a coherent API match. Phase 2 (per-pool wasm-history walk to confirm upgrade history) remains pending — but the current-state attribution + disassembly evidence is enough to flip `BackfillSafe=true` once an operator runs the per-pool walk to rule out historical decoder-incompatible upgrades.

## What "comprehensive" means after this update

We can now answer for **every contract** in our 532-input list (minus the 3 TTL-evicted anchors):

- **What WASM is it currently running?** ✅ (transitions where applicable + current-state RPC for stable contracts)
- **What was the upgrade history during the walked range?** ✅
- **What does the WASM expose?** ✅ (function exports + symbol strings via wasm-objdump for all 50 WASMs)
- **Which protocol does it belong to?** ✅ (classified via export signature, 100% of 50 hashes attributed)
- **What's the cascading effect of factory upgrades?** ✅ (factory upgrade ledgers + per-pool first-seen ledgers in `per-source-final/`)

## Audit-doc rot register (REVISED 2026-05-01)

⚠ **Reversal of an earlier finding.** The 2026-05-01 morning version of this report flagged
`aquarius.md`'s three cited hashes (`8875f0c7…`, `ae0da5a8…`, `f1077e0b…`) as "stale — none
match any hash in the current walk." That conclusion was wrong. After fetching the
current-state hash for the 283 walker-silent (`ranges:null`) contracts via Soroban-RPC, all
three audit-doc hashes show up — they belong to **168 Aquarius pools that never upgraded**,
which the wasm-history walker missed because it only emits transitions. The audit doc's
hash table reflects a real cohort of mainnet pools and is **not** doc-rotted.

What IS true: **the audit doc is incomplete, not wrong.** Aquarius has BOTH:

1. The 168-pool never-upgraded cohort (audit doc's three hashes).
2. The 145-pool upgrade-chain cohort (the b54ba37b → 4f080d24 chain documented
   above), which the audit doc didn't capture.

A refresh adding cohort (B) to `aquarius.md` is the right ask, not a rewrite.

## Tooling gaps surfaced

1. **`ratesengine-ops extract-wasm-from-galexie` returns MISSING for every hash.**
   Tested 2026-05-01 against multiple subsets of the 50 hashes and a 12M-ledger
   archive range. Tool reports zero matches. Workaround used in this audit: bypass
   the tool entirely, fetch WASM bytes via Soroban-RPC `getLedgerEntries` against
   the ContractCode LedgerKey. All 50 hashes resolved this way; SHA-256 of returned
   bytes matches the walker's reported hash 50/50. The extract tool has a real
   bug — the wasm-history walker reads the same archive cleanly so it's not an
   archive issue. Likely a `LedgerEntryChange`-discriminator mismatch or the
   `Created` change comes through under a different metadata variant. **Fix as a
   separate code change once the audit is in.**

2. **wasm-history walker only emits transitions, not initial state.** This caused
   the 283 false negatives (contracts with `ranges: null`) and led to my earlier
   incorrect "doc rot" conclusion on Aquarius. The walker should emit the
   *current* hash for any watched contract that had no transitions in the walked
   range, so a single output reflects the complete picture. Tracked.

3. **Three contracts (Aquarius router, Phoenix multihop anchor, Aquarius admin
   anchor) returned `not_found` from the RPC current-state query.** Their
   ContractInstance entries were TTL-evicted at query time. They're operationally
   live (we ingest through them) — TTL evict only the durable storage entry, not
   the contract address. To capture their WASM hashes we'd need either (a) to
   extend the entry's TTL via an invocation and re-query, or (b) to walk the
   archive for their deploy-ledger entry. Outside this audit's scope; document
   as residual.

4. **The 532-contract input list isn't curated in repo.** Future audits would
   have to reconstruct it. Curate `configs/audit/wasm-walk-contracts.yaml` per
   source with provenance.

## Operator next moves

| Action | Owner | Priority |
|---|---|---|
| Run per-pool wasm-history walk on r1 to confirm Blend pool upgrade history (Phase 2 of Task #53). Current-state attribution + disassembly already done in this audit | (operator on r1) | P1 |
| Fix `extract-wasm-from-galexie`'s LedgerEntryChange matching | (open) | P1 |
| Refresh `aquarius.md` to cover both pool cohorts (REVISED ask, not a rewrite) | ash | P2 |
| Curate `configs/audit/wasm-walk-contracts.yaml` from the 532 used here | ash | P2 |
| Re-walk after every Soroban-source upgrade detected via dispatcher logs | (operator) | recurring |

## Companion artefacts

In [`evidence/r1-walk-2026-05-01/`](evidence/r1-walk-2026-05-01/):

- `wasm-history-all.json`, `wasm-history-full.json` — raw walks
- `current-instance-state.json` — RPC-resolved current-state hashes for the 283 walker-silent contracts
- `wasm-bytes/<hash>.wasm` — **all 50 WASM byte files** (~1.8 MB total), SHA-256-verified
- `disasm/<hash>.json` — exports + imports + symbol-strings per WASM
- `classification.json` — per-WASM (source, role) tagging
- `per-source-final/{source}.json` — per-source comprehensive contract+history detail
- `per-source-final/hashes.json` — per-WASM contract-count + role
- `analyze.py`, `disasm.py`, `build-final.py`, `fetch-wasm-rpc.py` — analysis scripts (re-runnable)
