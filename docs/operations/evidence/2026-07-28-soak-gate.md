---
title: Evidence — galexie cold-tier trim 48h soak gate
last_verified: 2026-07-28
status: final — gate executed
---

# Galexie trim soak gate — PASSED + executed 2026-07-28 19:17Z

**Claim:** after trimming 780 parity-verified galexie-archive partitions
below ledger 49,984,000 (2026-07-26), the AWS-public cold tier kept
serving the trimmed span for 48h with zero misses, so the local ZFS
safety snapshot could be destroyed.

**Mechanism:** `galexie-soak-check.timer` (every 6h) →
`/usr/local/sbin/galexie-soak-check.sh` probes a rotating 18-ledger
sample across the trimmed span via `rehydrate -dry-run`, logging to
`/var/log/galexie-soak.log`.

**Result: 10 consecutive PASS, 0 FAIL** (gate required ≥8 PASS, 0 FAIL,
after 2026-07-28 17:00 UTC). Final log lines (timestamps CEST = UTC+2):

```
2026-07-28T11:20:44+02:00 PASS soak: 18 sampled, 0 missing_in_cold, 0 probe_errors
2026-07-28T17:20:44+02:00 PASS soak: 18 sampled, 0 missing_in_cold, 0 probe_errors
```

**Gate execution** (2026-07-28 19:17Z, auto-executed per the launch-loop
contract — time+evidence gate): `zfs destroy
data/minio@pre-trim-2026-07-26` succeeded; 0 pre-trim snapshots remain;
`galexie-soak-check.timer` disabled and removed.

**What this does NOT prove:** cold-tier durability going forward (AWS
could drift later — the archive-completeness daemon is the ongoing
detector), and rehydrate-based restore remains degraded until the
archivewriter credential fix lands (v1-launch-plan §2.2 / INBOX #1).
Rollback for any future cold-read gap is rehydrate-from-cold, not the
snapshot.
