# SEV-1 / SEV-2 drill record — 2026-06-13

Dry-run satisfying the Freighter RFP responsiveness SLAs (F3.5/F3.6) +
ctx-proposal Phase 6b ("Incident detection and alert validation").
Executed against the r1 production deployment in a controlled window with
no consumer traffic (live-in-development phase).

**SLA targets** (from `sev-playbook.md`):

| Sev | Detection | Notification | Triage |
|---|---|---|---|
| SEV-1 (downtime) | ≤ 15 min | ≤ 30 min | ≤ 30 min |
| SEV-2 (10× p95 / partial) | ≤ 30 min | ≤ 60 min | ≤ 240 min |

**Prerequisite verified before the drill:** the alerting layer was found
inert post-rebrand (Prometheus loaded a stale rule dir targeting the
removed `ratesengine-*` job names) and fixed 2026-06-13 — all 116 rules
now load with `stellarindex_*` targets. The `stellarindex_api_down` alert
(`for: 60s`, `severity=page → chat-page`) is confirmed loaded.

## SEV-1 drill — full API outage

Method: stop `stellarindex-api`, observe the alert pipeline transition
ACTIVE, record the detection latency, restore.

<!-- FILLED AT EXECUTION -->
| Event | Timestamp (UTC) | Δ from outage |
|---|---|---|
| API stopped (`systemctl stop stellarindex-api`) | T0= | 0 |
| `up{job="stellarindex-api"}` → 0 (next scrape) | | |
| `stellarindex_api_down` alert → PENDING | | |
| `stellarindex_api_down` alert → FIRING (after `for:60s`) | | |
| API restored (`systemctl start`) | | |
| `up` → 1, alert → resolved | | |

**Detection latency (outage → FIRING):** ___ s — **target ≤ 15 min**.
**Independent second channel:** the 5-min `r1-smoke.timer` /
Healthchecks.io API-smoke check (exit-code = failure count) also flags
the outage within ≤ 5 min (sev-playbook §3 requires two channels).

## SEV-2 drill — latency degradation (tabletop)

The `stellarindex_api_latency_p95_high` alert (`for: 2m`, expr p95 > the
SLO) covers the 10×-p95 SEV-2 trigger. A live 10× latency injection
isn't run against prod (it would require artificial load + risks real
degradation); the k6 `06-mixed-realistic` saturation run
(`test/load/reports/`) demonstrates the alert's expr fires when p95
crosses the threshold under load. Detection bound `for:2m` + 30s scrape
= ~2.5 min, **well within the SEV-2 ≤ 30 min target**.

## Verdict

<!-- FILLED AT EXECUTION: PASS/FAIL vs the two targets -->
