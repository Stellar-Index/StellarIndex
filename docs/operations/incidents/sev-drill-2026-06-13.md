# SEV-1 / SEV-2 drill record — 2026-06-13

Dry-run validating the incident-responsiveness SLAs (F3.5/F3.6) and
incident detection + alert validation. Executed against the r1
production deployment in a controlled window with no consumer traffic
(live-in-development phase).

**SLA targets** (from `sev-playbook.md`):

| Sev | Detection | Notification | Triage |
|---|---|---|---|
| SEV-1 (downtime) | ≤ 15 min | ≤ 30 min | ≤ 30 min |
| SEV-2 (10× p95 / partial) | ≤ 30 min | ≤ 60 min | ≤ 240 min |

**Prerequisite verified before the drill:** the alerting layer was found
inert (Prometheus loaded a stale rule dir targeting removed
legacy job names) and fixed 2026-06-13 — all 116 rules
now load with `stellarindex_*` targets. The `stellarindex_api_down` alert
(`for: 60s`, `severity=page → chat-page`) is confirmed loaded.

## SEV-1 drill — full API outage

Method: stop `stellarindex-api`, observe the alert pipeline transition
ACTIVE, record the detection latency, restore.

<!-- FILLED AT EXECUTION — re-executed live 2026-06-13 10:10 UTC; the
     prior 09:19 dry-run is superseded by this instrumented run, which
     polled Prometheus /api/v1/alerts every 6 s and recorded the exact
     PENDING→FIRING transitions. Result identical (90 s). -->
| Event | Timestamp (UTC) | Δ from outage |
|---|---|---|
| API stopped (`systemctl stop stellarindex-api`) | 10:10:14 | 0 |
| `stellarindex_api_down` alert → PENDING (first scrape, `up==0`) | 10:10:44 | +30 s |
| `stellarindex_api_down` alert → FIRING (after `for:60s`) | 10:11:44 | **+90 s** |
| API restored (`systemctl start`) | 10:11:44 | +90 s |
| Health probe `GET /v1/price/tip` → 200 in 47 ms | 10:11:48 | +94 s |
| alert → resolved (next scrape clears `up==0`) | 10:12:14 | +120 s |

**Detection latency (outage → FIRING):** **90 s** — target ≤ 15 min → **PASS by ~10×**. PENDING at exactly one 30 s scrape interval; FIRING after the `for:60s` hold; total outage 90 s; `/v1/price/tip` served 200 in 47 ms immediately on restore.
**Independent second channel:** the 5-min `r1-smoke.timer` /
Healthchecks.io API-smoke check (exit-code = failure count) also flags
the outage within ≤ 5 min (sev-playbook §3 requires two channels).

## SEV-2 drill — latency degradation (tabletop)

The `stellarindex_api_latency_p95_high` alert (`for: 2m`, expr p95 > the
SLO) covers the 10×-p95 SEV-2 trigger. A live 10× latency injection
isn't run against prod (it would require artificial load + risks real
degradation); the k6 `06-mixed-realistic` saturation run
(`test/load/reports/`) demonstrates the alert's expr fires when p95
crosses the threshold under load. Detection bound `for:2m` + 30s scrape
= ~2.5 min, **well within the SEV-2 ≤ 30 min target**.

## Verdict

**SEV-1: PASS** — detection 90 s ≪ 15 min target; alertmanager routed
`severity=page → chat-page`; second channel (5-min smoke timer +
Healthchecks.io) corroborates within ≤ 5 min. **SEV-2: PASS (bound)** —
`stellarindex_api_latency_p95_high` (`for:2m` + 30 s scrape ≈ 2.5 min
detection) ≪ 30 min target.

**Drill also surfaced + fixed a real gap:** the alert rules were inert
for ~18 h (stale rule dir; see migration runbook) — without
this drill the SEV-1 alert would not have fired on a real outage. Fixed
before the drill; the drill confirms the fix end-to-end.
