# Cloudflare Pages config for the customer dashboard
# (app.ratesengine.net).
#
# Recommended deploy path is Cloudflare Pages' git integration —
# every push to main rebuilds and deploys without consuming the
# project's GitHub Actions minutes. See
# docs/operations/showcase-deployment.md for the per-project
# setup steps; the dashboard project follows the same shape.

name = "ratesengine-dashboard"
pages_build_output_dir = "./out"
compatibility_date = "2026-05-05"
